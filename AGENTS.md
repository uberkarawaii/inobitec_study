# Ирина — программа подготовки

> Журнал работы и хранилище памяти AI-агента. Ведётся студенткой и агентом совместно. Наставник просматривает.
> Все договорённости, решения, журнал работы — здесь.
> Дату записей указывать явно.

## 0. Правила работы агента (мета)

1. **Память — только этот файл и студенческое рабочее пространство.** Системную memory не использовать.
2. **Язык — русский.**
3. **Перед запоминанием решения — фиксировать дату** в журнале (§ 7) и в соответствующей секции.

---

## 1. Контекст

- **Студентка:** Ирина Караваева.
- **Наставник:** Андрей Успехов (Inobitec, `auspehov@inobitec.com`).
- **Цель текущего периода:** подготовка и оценка навыков с прицелом на **стажировку в Inobitec**. По итогам программы наставник принимает решение: платная стажировка, либо продолжаем подготовку точечно по пробелам.
- **Старт:** июнь 2026.
- **Темп.** Параллельно — дипломная работа (плагин для T-FLEX CAD). Часы в неделю фиксируются на старте по договорённости с наставником; приоритеты определяет студентка. «Неделя» программы — это **этап по содержанию, не календарная неделя**: этап закрыт, когда закрыт; контрольная точка назначается по завершению этапа. Оценивается **динамика освоения на вложенное время**, не календарная скорость.
- **Рабочая ОС:** **Windows**. Стек — MSVC-сторона курса (см. § 2); материалы курса § 2.1.A покрывают обе стороны (MSVC и GCC/Clang) — работаем по MSVC-командам.
- **Формат работы:** задачи от наставника → решение с ИИ-ассистентом → артефакт в личном GitHub-репозитории → контрольная точка с наставником по завершению этапа. Вопросы между встречами — наставнику в Telegram.

## 2. Инструменты

- **OpenCode + DeepSeek** — ИИ-ассистент курса. Ключ выдаёт наставник (хранится у студентки, в этот файл не записывается). Запускается в консольном режиме, в папке проекта.
- **Этот файл (`AGENTS.md`)** — память агента. OpenCode читает его при каждом запуске и накапливает контекст работы. Сырые логи диалогов с DeepSeek — в `dialog_logs/`, обязательно (§ 8).
- **GitHub-репозиторий** — личный, доступ расшаривается на `auspehov@inobitec.com`. Все задачи — подкаталоги одного репозитория. **Регулярные коммиты по ходу работы** (см. § 4.1 Self-review) — история разработки складывается из мелких осмысленных шагов, не заливается одним коммитом постфактум.
- **Стартовый стек:** **FAR Manager** + **PowerShell** + **MSVC toolchain** (`cl`, `link`, `lib` из VS Build Tools, окружение через `vcvars64.bat` / «x64 Native Tools Command Prompt») + **`cdb`** (консольный отладчик из Debugging Tools for Windows) + git. **Без IDE на этом этапе**: Visual Studio используется только как поставщик toolchain'а, кнопка Build не используется — собираем руками из консоли. VS Code или полноценная VS подключатся позже, когда сборка и отладка из консоли освоены.
- **Что подключается по мере роста задач** (не сразу, см. § 5 про лестницу слоёв): пакетный сценарий (`.ps1`/`.bat` — переходный слой) → сборщик (**GNU make** + Makefile; на Windows ставится `winget install ezwinports.make`, версия 4.4+) → метасборщик (`CMake`) → пакетный менеджер (`vcpkg` — на Windows основной путь для C++-библиотек) → тестовый фреймворк (`GoogleTest`). Каждый слой — только когда задача его реально требует. *(У MSVC есть родной сборщик `nmake` — поставляется с VS; знай, что он существует, но в курсе используем GNU make: его синтаксис — индустриальный стандарт, Makefile получается переносимым между Windows и Linux.)*
- **Базовый фундамент сборки и отладки** (первые слои § 5): окружение и `vcvars64` → репо-инфраструктура (git + `.clang-format` + `.gitignore`) → hello world одной командой → базовые ключи компилятора и warnings (`/W4 /permissive-`) → юнит-тест как exe → отладочная печать → `cdb` → раздельные этапы сборки (`cl /c`, `link`, `lib`, `cl /LD` с собственной import-`.lib`). Режимы сборки — отдельная ось, § 4.7.
- **Сырые логи диалогов с DeepSeek** — `dialog_logs/YYYY-MM-DD_тема.json`, машинный экспорт штатной командой `opencode export <session-id>` (детали — § 8). Без обработки, без выжимок, без ручной транскрипции, без рефлексии пост-фактум. Наставник смотрит регулярно — это его основной канал оценки работы с ИИ.
- **Материалы курса наставника под рукой** (копии в этой папке, наставник синхронизирует вручную после правок в источнике):
  - [00-temy.md](00-temy.md) — каталог тем программной инженерии.
  - [01-teoriya.md](01-teoriya.md) — методика обучения, работа с ИИ, анти-подхалимажные приёмы (§ 1.2 — обязательно к применению, см. § 4.2).
  - [02-praktika.md](02-praktika.md) — таблица минимальных навыков C/C++ трека (§ 2.1.A — **MSVC-столбец наш**), методические принципы (§ 2.1.0), шпаргалка C++ vs C идиом (§ 2.1.B), стиль диагностики (§ 2.1.C), режимы сборки (§ 2.1.D), архитектура обработки ошибок (§ 2.1.F).
  - [03-ocenka.md](03-ocenka.md) — как оценивается работа.
- **Внешние ресурсы для углубления (регулярный просмотр обязателен):**
  - [**C++ Weekly** — Jason Turner на YouTube](https://www.youtube.com/@cppweekly) — 1–2 коротких видео в неделю + **одна идиома из видео применена в код недели** (см. § 4.3.A, ритуал).
  - [**C++ Best Practices**](https://github.com/cpp-best-practices/cppbestpractices) — сборник лучших практик.
  - [**cppreference**](https://en.cppreference.com/w/) — когда DeepSeek даёт сигнатуру или поведение — проверь там, не верь модели на слово.
  - [**Microsoft Learn: MSVC compiler options**](https://learn.microsoft.com/cpp/build/reference/compiler-options) — официальная справка по ключам `cl`/`link`/`lib`.
  - [**Debugging Tools for Windows (cdb/WinDbg)**](https://learn.microsoft.com/windows-hardware/drivers/debugger/) — команды отладчика, attach по PID, разбор дампов.

## 3. Стартовый промпт для DeepSeek

При первом запуске OpenCode в каждой новой сессии — задать контекст:

> Я — кандидатка на стажировку в Inobitec, прохожу программу подготовки. Наставник — Андрей Успехов. Сейчас работаю над задачей: **[краткая постановка из § 6]**. Стек: **Windows**, C17/C++23 (MSVC, `cl` с `/std:c17` / `/std:c++latest`), сборка и отладка из консоли (`vcvars64`, `cl /c`, `link`, `lib`, `cdb`), оболочка PowerShell, файловый менеджер FAR. `make`, `CMake`, `vcpkg`, `GoogleTest` ещё **не** подключены — не предполагай их использование, пока я явно не скажу. Предлагай идиомы C++23 там, где они короче и яснее: `std::print`/`std::println` вместо `std::cout`, `std::expected<T, E>` вместо ручных кодов возврата и исключений, structured bindings, ranges. Мой фон: университетские CAD-проекты — плагины для T-FLEX CAD на C# и C++/CLI, библиотека для КОМПАС-3D, приложение на MFC; увереннее всего я в C#; нативный C++ почти не писала, чистый C — не писала; собирала раньше только кнопкой Build в Visual Studio — консольный фундамент осваиваю с нуля, объясняй слои сборки, не перепрыгивай.
>
> Ты — мой помощник по обучению. Не делай всё за меня: подсказывай, объясняй, предлагай размышления. Приводи простые аналогии, где это в тему. Давай микро-практические работы для глубокого освоения материала.
>
> **Важно:** контролируй качество того, что советуешь. Если не уверен в API — отмечай явно («проверь по документации»). Не выдумывай функции и сигнатуры.
>
> Дополнительно применяй процедуры из § 4.2 моего AGENTS.md (анти-подхалимажный блок — копирую его следующей репликой).

## 4. Правила работы (важно)

### 4.1 Self-review перед коммитом

Перед каждым `git commit` / `git push`:

1. `git diff` пофайлово — пробежать глазами всё, что заливается.
2. Проверить: не залип ли мусор (артефакты сборки — `.obj`, `.exe`, `.pdb`, `.lib`, `.exp`, — временные файлы, логи прогонов, незавершённые комментарии, отладочные выводы, плейсхолдеры из ответов ИИ вроде «// ... существующий код ...»).
3. **Форматирование — механикой, не ручной дисциплиной.** Пока сборка ручная: `clang-format -i` на каждом изменённом файле + проверка `clang-format --dry-run -Werror` (падает на любом неотформатированном). Как только появляется `make` — обязательные цели `format` / `format-check`, и `test` не считается зелёным без формат-чистоты. `.clang-format` в корне репозитория — одна конфигурация на весь проект, **включая вспомогательные файлы** (генераторы, тесты): неважного кода в репо нет.
4. **Сборка с `/W4 /permissive-` зелёная — без warnings.** Дополнительно `/analyze` (статанализ MSVC) — каждое сообщение разбираем, не глушим.
5. **Тесты прогнаны в текущем рабочем режиме сборки** (§ 4.7). На 1-м этапе это Debug+ASan — единственная сборка. Если ASan ругается — фиксить, не заглушать.
6. Проект собирается с нуля одной командой (по мере лестницы: сценарий → `make` → `cmake --build`). **Вторая параллельная система сборки не заводится** — она неизбежно расходится с первой; переходный сценарий удаляется, когда появляется сборщик.
7. Тесты проходят и возвращают честный код: прогон обязан вернуть не-ноль, если упал хоть один кейс. Глотание ошибок в цикле прогона — дефект сборочной обвязки.
8. Все исправления по последнему ревью внесены. По каждому пункту ревью — короткий статус в переписке: **согласна/сделала · не согласна, потому что … · не поняла, вопрос**. Отклонение с аргументом — лучший ответ, чем молчаливое выполнение; молчаливый пропуск — худший.
9. **«Повторила дважды — выноси».** Если в diff один блок логики повторён два и более раз — перед коммитом вынести в функцию (или явно записать в коммит-сообщении, почему вынос сейчас неоправдан). Самопроверка: пробежать diff взглядом «что здесь похоже друг на друга?».
10. **Резервная копия кода — это git, не файл-копия.** Эксперимент — в ветке; состояние «до» — в теге или просто в истории. Закомментированные блоки «на всякий случай» и копии файлов рядом с проектом в коммит не идут: история и так всё хранит.

### 4.2 Анти-подхалимажные приёмы при работе с DeepSeek

DeepSeek склонен соглашаться и хвалить — это вредит обучению. Полный набор приёмов — [01-teoriya.md](01-teoriya.md) § 1.2 и [02-praktika.md](02-praktika.md) § 2.1.0.

**Блок для копирования второй репликой каждой новой сессии:**

> *Будь критичен и не доверяй утверждениям по умолчанию. Не соглашайся со мной автоматически: если я не права — скажи прямо. Помечай категорию [ФАКТ / МНЕНИЕ / ПРЕДПОЛОЖЕНИЕ] и уверенность [ВЫСОКИЙ / СРЕДНИЙ / НИЗКИЙ]. На вопросах про API сторонних библиотек ставь уверенность ниже и предлагай сверить с документацией.*
>
> *Перед тем как предлагать правку — задай вопрос: это локальная правка или нужен архитектурный пересмотр? Если у задачи есть архитектурные оси (модель ошибок, владение ресурсами, структура модулей, границы валидации) — назови их **до** правки.*
>
> *По итогу каждой ревизионной серии, прежде чем закрыть сессию, **сам инициируй финальную проверку**: «оцени уверенность в этом ревью 1–10; если не 10 — что могло остаться непроверенным?». Не жди, пока я спрошу. Оценка «10/10, претензий нет» — не финал, а триггер дополнительного раунда «найди ещё хоть одно».*
>
> *Если я отказываюсь от твоей рекомендации без явной причины — вернись к ней на финальной проверке.*

**Рабочие приёмы студентки:**

- **«Попытайся оспорить меня».** Сформулировала собственное инженерное решение — попроси модель его опровергнуть, выслушай контраргумент, прими решение **сама** (согласие с моделью не обязательно — но позиция должна быть аргументирована, «я так решила» не считается).
- **Отказ от рекомендации — помечай явно.** Если DeepSeek выдал чек-лист из N пунктов и применяешь часть, остальные не игнорируй молча: *«пункт 3 не делаю, причина: …»*.
- **Документация инструмента читается раньше вопросов к ИИ.** Перед вопросом про команду инструмента — `--help` / README / Microsoft Learn. Типовые промахи: «как закоммитить» (→ `git help commit`), «как экспортировать сессию» (→ `opencode --help`). К DeepSeek — когда документация неоднозначна, и тогда **с цитатой** из неё: «вот что написано, не понимаю X».
- **Трёхступенчатая проверка каждого совета ИИ.** (1) Локальный фикс применён, тесты прошли. (2) «Что я предполагала, когда писала этот фрагмент? Стоит ли ещё это предположение?» (3) «Зачем этот фрагмент вообще существует — решает реальную проблему или его можно удалить, а не улучшить?» Модель сама вопросы 2–3 не задаёт — это твоя работа. Частный случай: **перед заведением модуля/обёртки проверь, нет ли готового в STL** — обёртка без добавленной ценности не оправдывает модуль.
- **«Где ещё?» после каждой принятой правки.** Применила идиому или исправила дефект в одном месте — спроси себя (и модель): «в каких ещё местах моего кода та же проблема?» Исправление по списку без обобщения на собственный код — типовая ловушка; самопроверка — `grep` по своему diff'у на тот же паттерн.

### 4.3 STL обязательна на C++-стороне

C++-реализация — **не «C с классами»** и **не «C с `std::print`»**, а идиоматический C++:

- **Контейнеры** (`std::vector`, `std::array`, `std::string`, `std::map`) вместо C-массивов, `char[]`, ручного `malloc`.
- **STL-алгоритмы** (`std::ranges::sort`, `minmax_element`, `fold_left`, `views::filter`) вместо ручных циклов с индексом.
- **RAII** — умные указатели, контейнеры — вместо ручных init/destroy.
- **Range-based for** и **structured bindings** вместо итераторов с индексом.
- **`std::string`, `std::format`, `std::print`/`std::println`** вместо `char[]` + `sprintf`.
- **Обработка ошибок — `std::expected<T, E>` (C++23) и коды как основа; исключения — ознакомительно** (архитектурное решение курса, обоснование — [02-praktika.md](02-praktika.md) § 2.1.F: тренд C++23/26, production-практика крупных проектов, специфика стека Inobitec).

Шпаргалка соответствий «C ↔ C++» — [02-praktika.md](02-praktika.md) § 2.1.B.

C-реализация и C++-реализация одной задачи — **разные**, не похожие. C-сторона — для понимания низкоуровневых механик (память, errno, ручные буферы); C++-сторона — то, как пишут production-код.

**Про runtime-проверки инвариантов:** `assert()` как рабочий инструмент не используем (выключается в Release, даёт ложное чувство безопасности). Вместо него: Debug+ASan-сборка (§ 4.7), контракты как документация (`@pre`/`@post` в Doxygen-комментариях), ошибки, работающие и в Release (`std::expected`, явные коды), юнит-тесты на корнер-кейсы.

#### 4.3.A Типовые ловушки «C++ как C»

Самый частый паттерн при фоне «C++ из универа»: взять C-логику, заменить вывод на `std::print` — и считать это C++. На ревью наставник ловит именно это:

| Паттерн «как в C» | Идиоматичный C++ | Почему |
|---|---|---|
| `(int)v.size()` — C-style cast | `static_cast<...>` или, лучше, подходящий тип (`std::size_t`) на месте определения | C-cast молча делает любое преобразование; а каст-симптом часто маскирует неверно выбранный тип |
| `INFINITY` (макрос `<math.h>`) | `std::numeric_limits<double>::infinity()` | макрос обходит систему типов |
| `char buf[256]; sprintf(buf, ...)` | `std::format(...)` / `std::println(...)` | buffer overflow при первой неосторожности |
| ручной `while`-цикл подсчёта разрядов числа | `std::formatted_size("{}", n)` | STL уже умеет длину форматированной строки |
| три прохода `min`/`max`/`sum` или ручной цикл-свёртка | `std::ranges::minmax_element` + `std::ranges::fold_left` | алгоритмы передают намерение и снимают классы ошибок инициализации свёртки целиком |
| `return std::string("лет");` из литерала | `std::string_view` | без аллокации на каждый вызов |
| `for (int i = 0; i < v.size(); ++i)` | `for (auto& x : v)` / ranges | индексный цикл — наследие C |
| `enum { RED, GREEN }` | `enum class Color { Red, Green }` | изоляция имён и типобезопасность |
| `#define MAX_N 100` | `constexpr int kMaxN = 100;` | проверяется компилятором, имеет scope |
| `void f(double* arr, size_t n)` | `void f(std::span<const double> arr)` | длина едет вместе с данными |
| обёртка `is_greater(x, k)` в своём модуле | `std::bind_back(std::greater{}, k)` | предикат и partial application уже в STL — модуль не нужен |

#### 4.3.B Типовые ловушки «C++ как C#»

При фоне C# перекос другой — и его наставник ловит так же. Главное отличие сред: в C# за временем жизни следит GC, в C++ — ты, через RAII; в C# всё — класс, в C++ класс нужен только там, где есть инвариант.

| Паттерн «как в C#» | Идиоматичный C++ | Почему |
|---|---|---|
| статический класс-хелпер на каждую группу функций | свободные функции в `namespace` | класс без состояния и инварианта — лишняя обёртка |
| проверка типа строкой (`GetType().Name == "..."`) | виртуальные функции / `std::variant` + `std::visit`; в крайнем случае `dynamic_cast` | строка не проверяется компилятором — опечатка молча ломает ветку |
| `try { ... } catch { }` «чтобы не упало» | узкий `catch` конкретного типа с обработкой, или не ловить вовсе; основная модель курса — `std::expected` | проглоченная ошибка всплывает дальше от причины и дороже |
| парсинг числа через подмену культуры (`Replace('.', ',')`) | `std::from_chars` | локале-независим по стандарту, без строковых трюков |
| `null`-проверки как основная защита | владение через типы: значение, ссылка, `std::optional`, умный указатель | система типов выражает «может отсутствовать» явно |
| глобальное статическое состояние между вызовами | состояние — в объекте, время жизни которого явно | у плагина/программы может быть два контекста сразу — статика молча затирается |

**Ритуал «после написания — проверить идиоматичность».** Когда версия работает, перед коммитом — запрос к DeepSeek:

> *Проверь этот код на C-наследие (C-cast, ручные циклы вместо алгоритмов, макросы вместо `constexpr`, ручные буферы вместо `std::format`) и на C#-привычки (статические хелперы вместо свободных функций, проверки типов строками, широкие catch). Покажи конкретные строки, предложи идиоматичную замену, объясни, почему она лучше.*

**Ритуал недели:** 1–2 видео C++ Weekly + одна идиома из них применена в код этапа. На контрольной точке наставник спросит: «какое видео смотрела, где применила?»

### 4.4 Параллельная реализация C и C++

Задачи 1–3 решаются **дважды**: на чистом C (C17) и на C++ (C++23). Цель — увидеть разницу подходов: `malloc/free` vs RAII; `errno` и коды vs `std::expected`; индексные циклы vs ranges; `printf` vs `std::print`; `char*` vs `std::string`.

Чистый C для тебя — **новый материал** (фон: C# и управляемый C++/CLI). Это нормально и учтено в темпе: C-сторона задач пойдёт медленнее C++-стороны, на ней закладывается понимание того, что C++ и тем более C# автоматизируют. Не проскакивай её через «DeepSeek, напиши C-версию» — именно тут видно усвоение.

### 4.5 Контрольные точки (по завершению этапа, очно)

Встреча с наставником ~2 часа, день договариваем гибко. Формат:

1. **Демонстрация результата этапа:** репозиторий, артефакт работает, тесты проходят.
2. **Работа на чистой виртуальной машине (Windows), которую даёт наставник:**
   - **Установка окружения с нуля** — VS Build Tools (или согласованный набор), FAR, git; `vcvars64`, проверка `cl`, ручная сборка hello world через `cl /c hello.cpp` → `link hello.obj`. Время этого этапа — само по себе показатель освоенности фундамента.
   - **Повтор ключевого куска задачи без ИИ + вариация от наставника.** Без OpenCode, без DeepSeek, без любых ИИ. **Интернет можно** — cppreference, Microsoft Learn, Stack Overflow. Наставник наблюдает за работой: команды, шорткаты, ориентация в FAR, чтение диагностики компилятора, выбор инструментов отладки.
3. **Краткое обсуждение** диалогов с DeepSeek за этап — где помог, где галлюцинировал, как ты это заметила.

**Это не экзамен.** Это active recall: если ты поняла свой код, написанный с ИИ, — ты его воспроизведёшь. Если прогнала через ИИ и приняла — повтор не пойдёт. Знание этого меняет твою работу весь этап: вникай, не делегируй слепо. Письменные ответы и пересказы каналом проверки не являются — только руки на чистой машине.

### 4.6 Стиль диагностических сообщений

Сообщение об ошибке — конкретика, не штамп: **что не получилось, какое значение получено, какое допустимо**. Без эмоций, без обвинений, без префиксов «Ошибка:», «Error:».

Плохо: `Ошибка, число не в диапазоне`. Плохо: `ошибка ввода: сбой потока`.
Хорошо: `N должно быть в [1; 20]; получено: 42`.
Хорошо: `не удалось прочитать stdin: <текст системной ошибки> (errno <код>)`.

Полное правило — [02-praktika.md](02-praktika.md) § 2.1.C. Применяется **к каждому диагностическому сообщению с первой задачи, включая новый код** — не только к местам, указанным в ревью. Самопроверка перед коммитом: `grep -nE "Ошибка|Error|Failed|Не удалось[^:]*$|сбой"` по diff'у — каждое совпадение пересмотреть.

Это не косметика: на ревью наставник смотрит сообщения об ошибках первым делом.

### 4.7 Режимы сборки (MSVC)

Сборка — последовательность подключения режимов по мере роста задачи, не «выбери один из четырёх». Полная матрица и соответствие GCC-стороне — [02-praktika.md](02-praktika.md) § 2.1.D.

**Ступень 1. Debug + ASan — стартовый и единственный режим 1-го этапа.**

Флаги: `cl /std:c++latest /W4 /permissive- /EHsc /Od /Zi /MDd /fsanitize=address`

- `/Od /Zi` — без оптимизаций, полная отладочная информация (pdb): отладчик идёт по строкам 1:1.
- `/fsanitize=address` — ASan: out-of-bounds, use-after-free; ловит баги в момент возникновения.
- `/MDd` — отладочный CRT + проверки итераторов STL (`_ITERATOR_DEBUG_LEVEL=2` по умолчанию).
- UBSan в MSVC отсутствует — классы UB частично закрывает `/analyze`; при необходимости подключим `clang-cl` (сверяться с § 2.1.D — состояние инструментов меняется).
- **Бенчмарки на Debug не делать** — медленнее в разы; замеры — только ступень 3.

**Ступень 2. Coverage — когда появились тесты.** Отдельная сборка и прогон через `Microsoft.CodeCoverage.Console`, отчёт с разбором непокрытых веток. Детали — на этапе 3.

**Ступень 3. Release — когда нужно мерить производительность.**

Флаги: `cl /O2 /Zi /DNDEBUG /MD` + линкер `/DEBUG /OPT:REF /OPT:ICF`.

**`/Zi` + `/DEBUG` обязательны и в Release — pdb всегда лежит рядом с exe.** Без pdb crash в production = голые адреса в стеке. pdb пользователю не отдаётся, но обязательно сохраняется.

**Ступень 4. Production — финальный артефакт.** Стратегия поставки CRT: `/MD` (динамический CRT, требует VC Redistributable на целевой машине) vs `/MT` (статический CRT, автономный exe большего размера) — это **часть проектного дизайна**. PGO для CPU-bound: `/GL` + `/LTCG:PGI` → нагрузочный прогон → `/LTCG:PGO`. Постмортем-цепочка: дамп (`procdump -ma`) → `cdb -z crash.dmp` + pdb → стек с именами.

**Ось конфигурации в сборке — закладывается заранее:** когда появится `make`/`CMake`, режим (Debug/Release/Coverage) — параметр сборки с раздельными каталогами артефактов, не правка флагов на месте. Иначе при вводе Release сборку придётся переписывать, а не расширять.

### 4.8 Спираль освоения: голая версия с первого дня, промышленная позже

Каждый инфраструктурный инструмент имеет **учебный голый аналог** из стандартной библиотеки или базового toolchain'а. Голая версия — **с первой задачи**; промышленная — когда задача доросла. Привычка пользоваться инструментом формируется в работе с ним, не «после установки настоящего».

| Тема | Голая (с 1-го этапа) | Промышленная (позже) | Когда переход |
|---|---|---|---|
| Юнит-тестирование | exe с `return 0/1` | GoogleTest | тестов 10+, нужны параметризованные |
| E2E CLI | `prog < in > out` + `fc`-сравнение с эталоном | матричный suite на GoogleTest + `boost::process` | сценариев 10+, интеграция в `ctest` |
| Логирование | `std::println(stderr, ...)` без обёрток | `spdlog` | нужны уровни/ротация/асинхронность |
| Версионирование | `kVersion` + `--version` + `CHANGELOG.md` | CMake `project(VERSION)` + git describe | появилась CMake-сборка |
| Сборка | `cl` руками → раздельные этапы | `.ps1` (переходно) → GNU make → CMake | второй модуль / надоело руками |
| Статанализ | `/W4 /permissive-` | `/analyze` → `clang-tidy` | сборка чиста по warnings |
| Форматирование | `.clang-format` с первого коммита | цели `format`/`format-check` в сборщике, CI | появился сборщик |

**Критерий лог-строки:** «что я захочу знать, читая этот лог после сбоя?» В лог идут факты, иначе не видимые: объёмы, длительности, переходы состояний. **Эхо пользовательского ввода — шум**; в коротких интерактивных программах логировать почти нечего, и это честный вывод, а не недоработка. Персональные данные в лог не пишутся (в медицинском ПО имя пациента в логе — инцидент): протоколируется факт события, не содержимое.

На ревью наставник смотрит: есть ли голая версия каждого инструмента уже на 1-м этапе. Промышленная версия позже **не компенсирует** отсутствие голой сейчас.

### 4.9 Политики сборки и тестов (действуют с первого дня)

1. **Одна точка входа в проект.** Вся цепочка «сборка → генерация входов → эталоны → прогоны → сравнение → формат» — воспроизводима одной командой. До сборщика — минимальный переходный сценарий; с появлением `make` — цели make с честными зависимостями, переходный сценарий удаляется. Проверка: на чистом клоне до зелёных тестов — одна команда, `make test`.
2. **Скриптовые полотна минимизированы.** Связка в одну строчку (в рецепте цели) — допустима; логика длиннее — пишется тестом на C/C++ (exe с `return 0/1`). Оркестрация на десятки строк в `.ps1`/`.bat` не заводится: она не видна ни компилятору, ни санитайзеру, и непереносима.
3. **Эталоны генерятся на лету, в репо не хранятся.** В репозитории — только исходники (генератор, программа, референс) и сборочные файлы. Эталон считает **независимая референс-реализация** (однострочник PowerShell / отдельный простой exe — не прогон тестируемой программы, иначе тест сравнивает её саму с собой). Замороженный снапшот выхода — не эталон корректности: он фиксирует и ошибку, если она была в первом прогоне. Сгенерированные данные живут в `build/` под `.gitignore`.
4. **Кроссплатформенность по умолчанию.** Платформо-специфичный код — в `#ifdef` (`_WIN32` / `__unix__` / `__APPLE__`); предпочитать стандартные альтернативы (`std::filesystem`, `std::chrono`, `std::format`, `std::thread`); типы фиксированной ширины (`int32_t`, `std::size_t`). **Сборочное описание — тоже кроссплатформенное**: Makefile пишется на GNU-make-синтаксисе с toolchain-различиями (MSVC / GCC) в условных блоках переменных, не двумя параллельными системами сборки (см. слой 15). Допустимо начать с одного заполненного toolchain-блока (MSVC) — с явной фиксацией в README, что GCC-блок не реализован.
5. **Магические числа именуются.** Числа с доменным смыслом — в именованные константы (`constexpr` в C++, `enum`/`#define` с префиксом проекта в C). Exit-коды — один раз в общем заголовке (`common/exit_codes.h(pp)`), не рассыпаны литералами. Обратный карго-культ (`kZero = 0`) не нужен. **Пути — никогда не абсолютные литералы**: рабочие файлы ищутся относительно явно определённой базы (аргумент, переменная окружения, каталог exe), и это решение фиксируется в README.
6. **Exit-коды дифференцированы с первой задачи.** Ориентир-семантика sysexits: 64 — usage (плохие аргументы), 65 — данные (не-число в потоке), 66 — нет входных данных, 74 — IO-сбой. На Windows `<sysexits.h>` нет — значения задаются своими константами в `common/`. **Двухканальность:** exit-код — категория для скрипта-обёртки (машинный канал), сообщение в stderr — детализация для человека (системный текст ошибки + контекст операции). Антипаттерн — `return errno;`.
7. **Тесты проверяют все три канала:** stdout (рабочий вывод), stderr (диагностика — не глушить `2>$null`, иначе штампы в сообщениях невидимы для тестов), exit-код (**именно ожидаемый**, не «не ноль» — иначе дифференциация отвалится молча). Ошибочные ветки (IO-сбой, пустой ввод) — тоже провоцируются тестами: код, который никогда не исполнялся, — код на доверии.

### 4.10 Диагностика до причины: обходной путь — не финал

Когда что-то «не работает, а вот так — работает», у расхождения есть причина, и она дешевле всего находится сейчас, пока контекст горячий. Правило курса:

**Процедура при любом «почему-то не работает»:**

1. **Воспроизвести минимально** — отрезать всё лишнее, пока сбой не останется в нескольких строках.
2. **Прочитать точный текст ошибки целиком** — код, имя файла, номер строки; не пересказ «что-то про линковку».
3. **Сформулировать гипотезу** — «думаю, дело в X, потому что Y» — и проверить именно её.
4. Только после этого — фикс. Фикс без подтверждённой гипотезы — это лотерея.

**Вопрос к ИИ — с точным текстом ошибки и вопросом «почему», не «сделай по-другому».** На «не работает, предложи другой способ» модель охотно выдаст обходной путь — и причина останется в коде миной. На «вот точная ошибка, вот минимальный пример — в чём причина?» — шанс понять.

**Обходной путь допустим только как явная временная заглушка:** рядом — комментарий `// TODO: обходной путь, причина не найдена; пробовала A и B, гипотеза C`, и запись в журнале (§ 7). Перед закрытием этапа каждый такой TODO либо заменён пониманием причины, либо вынесен вопросом наставнику. Типовые сигналы, что в коде поселился непонятый обходной путь: абсолютный путь «потому что иначе ошибка»; закомментированный блок «это не вышло»; подкручивание значений до совпадения; «работает — не трогаю».

**Таймбокс честных попыток.** Если после 3–4 итераций «гипотеза → проверка» причина не найдена — это не провал, а момент зафиксировать в журнале «что пробовала, что отбросила, где застряла» и спросить наставника. Вопрос с историей попыток — сильный ход, не слабость; молчаливый костыль — наоборот.

На контрольных точках наставник будет спрашивать «почему» по местам из твоего же кода — это проверяемая часть программы, наравне с тестами.

## 5. Лестница слоёв (Windows-flavor, ориентир)

Слои подключаются, когда задача доросла. **Не пропускаем слои**: если на текущем буксует — закрываем, не двигаемся дальше; перепрыгивание создаёт карго-культы (CMake поверх непонятого `cl /c` и `link`). Ссылки на слои — по имени. Развёрнутые описания и GCC-параллели — [02-praktika.md](02-praktika.md) § 2.1.A.

1. **Окружение и toolchain.** VS Build Tools, `vcvars64.bat` (или Native Tools Prompt — понять, что он делает с `PATH`/`INCLUDE`/`LIB`), FAR, git. Проверка: `cl` из консоли отдаёт версию.
2. **Репо-инфраструктура.** `git init` + `.gitignore` (`*.obj`, `*.exe`, `*.pdb`, `*.lib`, `*.dll`, `*.pch`, `build/`) + `.clang-format` + `.editorconfig` + минимальный `README.md` — **раньше первого hello world**: первый коммит формат-чистый. README — артефакт портфолио: описание, структура, как собрать, как прогнать тесты; поддерживается актуальным.
   - **Защита заголовочников — дифференцированно по языку:** `.h` (C) → `#ifndef`-guards с уникальным префиксом проекта (`IRINA_T1_GEOM_CORE_H`); `.hpp` (C++) → `#pragma once`. Единый стиль на каждый тип файла во всём репо.
   - **Порядок `#include`** (Google-стиль): свой header первым (тест self-containment) → пустая строка → C system → C++ stdlib → сторонние → проектные. Каждый `.cpp` явно включает всё, что использует, — не полагается на транзитивные.
3. **Hello world одной командой** — `cl hello.cpp` → `hello.exe`. Первый коммит.
4. **Базовый I/O.** `std::cin`/`cout`/`cerr`, `std::getline` (ловушка `\n` после `>>`), `cin.fail()`, чтение до EOF, `std::format`/`std::println`. Перенаправление PowerShell/cmd (`<`, `>`, `|`).
   - **Кодировка: стандарт слоя — CP1251.** Исходники с русскими литералами сохранять в CP1251 (FAR Editor умеет), консоль — `chcp 1251`, в коде `setlocale(LC_ALL, "Russian_Russia.1251")` первой строкой `main()` — тогда `<ctype.h>` корректен на русских буквах, `name[i]` — символ. **`chcp 65001` (UTF-8) для интерактивного ввода русских букв нестабилен** (известный дефект моста CRT↔консоль: pipe-тесты проходят, живой ввод теряется) — не использовать до слоя локализации. `setlocale` ставится осознанно: убери его и посмотри, что изменилось, — если ничего, значит в этом месте он не нужен. **Кодировка всех исходников репозитория — одна, зафиксирована в `.editorconfig`**; смесь CP1251/UTF-8 по файлам — дефект репо-инфраструктуры, ловится на ревью.
5. **Базовые ключи и warnings.** `/W4 /permissive- /std:c++latest /EHsc /Od /Zi`. Каждое предупреждение разбираем.
6. **Тестирование без фреймворка.** (6a) юнит-тест функции = отдельный exe с `return 0/1`; (6b) acceptance: `prog < test_data.txt > out.txt` + `fc out.txt etalon.txt` — с учётом политик § 4.9 (эталон на лету, проверка stderr и exit-кода).
7. **Отладочная печать.** `std::println(stderr, "x={}", x)` — временный инструмент, удаляется перед коммитом (в отличие от протоколирования).
8. **Отладчик `cdb`.** Breakpoints, step, стек, перенаправление stdin при запуске под отладчиком. Уровни владения: запуск с `< file` → отладка с конвейерным входом → attach по PID. Если при баге первым делом тянешься к `printf` — отладчик ещё не освоен.
9. **Раздельные этапы сборки.** `cl /c file.cpp` → `.obj`; `link main.obj foo.obj /OUT:prog.exe`; `lib /OUT:mylib.lib *.obj` (статическая); **`cl /LD utils.cpp` → `.dll` + import-`.lib`, линковка потребителя через import-lib** — обязательный пункт фундамента, не опция. Подключается, когда у задачи появился второй модуль. *(Здесь же раз и навсегда проясняется, что такое `.obj`, `.pdb`, `.lib`, `.exp`, `.ilk` — и почему всем им место в `.gitignore`, а не в репозитории.)*
10. **UX и справка CLI.** `argc`/`argv`, `--help`, `--version`, разделение stdout/stderr, дифференцированные exit-коды (§ 4.9 п. 6), pipe-friendly поведение.
11. **Голое протоколирование** (§ 4.8, критерий лог-строки там же).
12. **Голое версионирование.** `kVersion` + `--version` + `CHANGELOG.md` (Keep a Changelog).
13. **Пакетный сценарий `.ps1`** — переходный слой: когда `cl` руками надоел. Живёт до сборщика, удаляется при появлении `make`.
14. **PCH.** MSVC-механика: `/Yc"pch.h"` (создать) / `/Yu"pch.h"` (использовать) — stdafx-стиль; либо force-include `/FI`. **Две когерентные схемы**: или pch включается первой строкой и дубли заголовков убраны, или pch только в флагах сборки и исходники держат реальные зависимости. Гибрид «включаю pch И дублирую те же заголовки» бессмыслен. В pch — только стабильные тяжёлые заголовки (STL, SDK), не свои часто-правимые: иначе каскадная пересборка. Польза измеряется секундомером (`Measure-Command` на полной пересборке с pch и без), не предполагается.
15. **Сборщик GNU make.** На Windows ставится `winget install ezwinports.make` (4.4+), запускается из того же Native Tools-окружения, что и `cl`. Родной для MSVC `nmake` в курсе не используем: его синтаксис (без pattern rules, свои инференс-правила) никуда не переносится, а GNU-make-синтаксис — индустриальный стандарт. Главный смысл слоя — **граф зависимостей как модель**: цель → пререквизиты → рецепт, пересборка только изменившегося; pattern rules (`build/%.obj: %.cpp`), переменные, `.PHONY`, order-only пререквизиты. **Данные тестов — тоже узлы графа** (вход — цель от генератора, выход — цель от входа и бинаря), не побочный продукт скрипта.
    - **Кроссплатформенность Makefile решается на этом слое, а не откладывается до CMake.** Один Makefile, граф и цели общие; toolchain-различия — в условных блоках переменных (`ifeq`): компилятор (`cl` / `g++`), флаги (`/W4 /EHsc /Fo` vs `-Wall -o`), суффиксы (`.obj`/`.exe` vs `.o`/пусто), команды сравнения (`fc` / `diff`). Рецепты — переносимые однострочники через эти переменные, без shell-измов (логика длиннее однострочника — это тест-exe, § 4.9 п. 2). Проверка слоя: тот же Makefile с заполненным GCC-блоком собрал бы проект на Linux.
    - **Header-зависимости:** на GCC-стороне это автоматика `-MMD -MP`; у `cl` аналога нет — есть `/showIncludes` (парсинг его вывода — продвинутое упражнение). На учебном масштабе деп-листы на MSVC ведутся руками — и протухающая ручная зависимость, которую забыли обновить, — честная мотивация следующего слоя.
16. **Метасборщик CMake.** `CMakeLists.txt` → `cmake -B build` → `cmake --build build`. Мотивация после переносимого Makefile — не «переносимость» (она уже есть), а: автоматические header-зависимости (то, что на слое 15 делалось руками), абстракция toolchain-флагов (ifeq-блоки исчезают), `find_package` + vcpkg для зависимостей, `target_precompile_headers`, генерация под разные сборщики и IDE.
17. **E2E с генератором.** `gen_N.cpp` (`std::mt19937` с фиксированным seed) для массовых входов; эталон — независимый референс (§ 4.9 п. 3).
18. **Пакетный менеджер `vcpkg`** (manifest-mode). Первая библиотека — собрать один раз вручную, чтобы увидеть, что vcpkg автоматизирует.
19. **GoogleTest** → 20. **Матричный CLI-acceptance (GoogleTest + `boost::process`)** — инфраструктура тестирования строится полностью **до** оптимизаций: меняя алгоритм, прогоняем suite и видим, не сломалось ли поведение; без полной testbed оптимизация — слепая работа → 21. **Покрытие** (`Microsoft.CodeCoverage.Console`) → 22–23. **Статанализ** (`/analyze`, `clang-tidy`) → 24. **Doxygen** → 25. **Локализация** (ru/en по локали; здесь — момент пересмотра кодировки).
26. **Оценка алгоритмической сложности и адаптация под ресурсы — рефакторинг производительности.** Лестница «корректно → масштабируемо → быстро алгоритмически → быстро параллельно». Big-O-анализ; выбор алгоритма под сценарий-ограничение («мало памяти / много данных / слабый CPU» — переключатель алгоритма, не просто «оптимизируй»); замеры `std::chrono` **только на Release** (§ 4.7 ступень 3 — на этом слое Release появляется впервые). Обязательный артефакт — **таблица замеров `N → время` по всем вариантам лестницы**: где какой вариант начинает выигрывать и почему; расхождение с теорией — повод искать причину, не игнорировать. Дисциплина **тестово-управляемой оптимизации**: меняем алгоритм → тестовый suite зелёный (поведение не изменилось) → замер → решение оставить/откатить. Цель `bench` в Makefile: большой вход от генератора (`--size`/`--seed`) + замер времени = реперная точка до и после каждого шага. Решение об оптимизации принимается **по замерам на конкретном профиле нагрузки**, не «интуитивно по виду кода»; эквивалентный по логике код может иметь разную скорость из-за микроархитектуры CPU.
27. **Параллелизм — глобальный навык, последняя ступень лестницы оптимизаций** (после корректной и алгоритмически быстрой версии — параллелить медленный алгоритм бессмысленно). Прогрессия: **Parallel STL** (`std::execution::par` в `std::sort`/`for_each`/`reduce` — одна строка, MSVC поддерживает из коробки) → **`std::thread` / `std::jthread` / `std::async`** (ручное разбиение работы, агрегация результатов) → **OpenMP** (`#pragma omp parallel for`, в MSVC — ключ `/openmp`) → SIMD (опционально). Обязательное понимание: data races, deadlocks, false sharing; когда параллелизм **не** ускоряет (IO-bound, мелкие задачи — оверхед запуска потоков съедает выигрыш). Каждая параллельная версия — строка в таблице замеров слоя 26 рядом с последовательной; для многопоточного кода — TSan-класс проверок (на MSVC ASan гонки не ловит — анализ через `/analyze` и дисциплину, на CMake-слое возможен прогон под clang).
28. **PGO** (§ 4.7 ступень 4): `/GL /LTCG:PGI` → нагрузочный прогон на репрезентативном входе → `/LTCG:PGO`; 5–15 % на CPU-bound «бесплатно», на IO-bound прироста нет — и это тоже проверяется замером, не предполагается.
29. **Пакет утилит с subcommands.**

Блок production-эксплуатации (промышленное логирование, версионирование через CMake, стратегия поставки CRT + pdb, краш-менеджер, постмортем `cdb -z`) — карта роста после оценочного периода, в этапы 1–4 не входит.

## 6. Постановки задач

> Базовый набор — § 6.1, фиксированный. По итогам каждой контрольной точки на эти же задачи накладывается новый слой: **этап 2** — CMake/vcpkg/GoogleTest + Release-сборка и **лестница оптимизаций с параллелизмом** (слои 26–28: корректно → масштабируемо → быстро алгоритмически → быстро параллельно, таблица замеров, PGO); **этап 3** — UX/локализация + покрытие тестами; **этап 4** — пакет утилит + матричный acceptance; **финал** — «вторая итерация» (§ 6.5). Новые постановки не добавляются — наращиваются слои на существующих.
>
> Рефакторинг в программе — три постоянных вида, не разовое событие: **качества** (идиоматический — ритуалы § 4.3.A/4.3.B после каждой работающей версии), **производительности** (тестово-управляемая оптимизация — слой 26, этап 2), **эволюционный** (возврат к закрытой задаче с изменённым условием — § 6.5, финал).

### 6.1 Базовый набор: геометрия точек — ввод/вывод и аргументы командной строки

Сквозной домен задачника — **точки в 3D** (`x y z`, вещественные). Каждая задача решается дважды — C17 и C++23 (MSVC, `/std:c17` / `/std:c++latest`), режим сборки — Debug+ASan (§ 4.7 ступень 1). Сборка пока вручную: `cl /c` → `link`.

**Сколько успеть на 1-м этапе:** базовая планка — 3 задачи, хорошая — 4, отличная — 4 + полная тестовая цепочка к каждой. Темп определит план дальше; буксование на слое — сигнал закрыть слой, не провал.

**Задача 1. Матрица попарных расстояний.** Дано целое `N` (3 ≤ N ≤ 20). Построить вершины правильного `N`-угольника единичного радиуса с центром в начале координат (в плоскости `z = 0`) и вывести матрицу `N×N` попарных расстояний между вершинами, выровненную по столбцам, с 3 знаками после запятой. Диагональ — нули, матрица симметрична. `N` вне диапазона или не число → диагностика по § 4.6 в stderr, ненулевой код возврата.

**Задача 2. Паспорт фигуры.** Программа запрашивает название фигуры (строка, русские буквы — может быть из нескольких слов) и число вершин `V`, выводит «Фигура «звезда»: 7 вершин.» с правильным склонением **«вершина / вершины / вершин»** по последней цифре. *(Первое столкновение с кодировкой: исходник в CP1251, консоль `chcp 1251`, `setlocale` — см. слой 4. Пустое имя и EOF — два разных случая с разной диагностикой.)*

**Задача 3. Bounding box и центроид облака точек.** Читает из stdin точки `x y z` (по одной в строке) до EOF. Выводит: количество точек, min/max по каждой из трёх осей (ограничивающий параллелепипед), центроид, среднее расстояние от точек до центроида (3 знака). Корректно работает через перенаправление: `prog < cloud.txt > report.txt`. Классы ошибок различаются по § 4.9 п. 6: не-число посреди потока (65) / пустой ввод (66) / сбой IO (74) — с конкретной диагностикой (номер битой строки — в сообщение).

**Задача 4. Фильтр точек по радиусу.** Первым аргументом — вещественное `R > 0`. Из stdin читает точки до EOF, в stdout выводит только те, чьё расстояние от начала координат меньше `R`, по одной в строке, в том же формате `x y z`. Аргумент не задан / не число / не положителен → код 64 с диагностикой по § 4.6 в stderr.

**К каждой задаче — тестовая цепочка** (без неё задача не завершена, даже если работает на ручных входах), по политикам § 4.9:

- **Acceptance (6b, все задачи):** вход — от генератора или короткий литерал; эталон — независимая референс-реализация на лету (для задачи 1 — формула длины хорды `2·sin(π·k/N)`; для задач 3–4 — однострочник PowerShell или отдельный простой exe-референс); сравнение `fc`; проверка stdout, stderr и **конкретного** exit-кода. Корнер-кейсы (пустой ввод, граница диапазона, не-число, точка ровно на радиусе `R`) — отдельными кейсами.
- **Массовый вход (задачи 3–4):** генератор облака точек `gen_cloud --size --seed` (`std::mt19937`, фиксированный seed), размеры 10 / 100 / 10⁶; данные в `build/` под `.gitignore`.
- **Юнит-уровень (6a):** чистая логика — склонение (`vertex_word`), геометрическое ядро (расстояние, центроид, вершина N-угольника), парсинг `R` — exe с `return 0/1`.

**Сравнение вещественных в тестах:** расстояния и центроиды — float-арифметика; эталон и результат сравниваются с фиксированным форматом вывода (3 знака — тогда `fc` честен) либо с допуском в exe-тесте. Побайтовое сравнение неокруглённых double — ловушка, не делать.

**Что осваивается по ходу:** потоки и форматирование, ловушка `\n` после `>>`, цикл до EOF, перенаправление (увидеть: программа не знает про файлы — это shell их подсунул), `argc`/`argv`, парсинг строки в число (`std::from_chars` → `std::expected` как целевая идиома), `std::isfinite` и дисциплина работы с вещественными, коды возврата, stderr.

**Задел на этапы вперёд:** общая геометрическая логика задач 3–4 (точка, парсинг строки `x y z`, расстояние) — кандидат на общий модуль при появлении реальной общей логики (слой 9; обёртка над одной строкой STL модулем не считается — § 4.2, трёхступенчатая проверка). На этапе 2 эти же задачи — материал лестницы оптимизаций (попарные расстояния O(N²) → пространственная сетка → параллельно); на этапе 4 — пакет `geom` с subcommands.

**Что НЕ подключается сейчас** (рано): `fopen`/`ifstream` (хватает перенаправления), CLI11, GoogleTest, CMake, vcpkg.

### 6.2–6.4 Задачи этапов 2–4

*(Постановки добавляются наставником по итогам контрольной точки предыдущего этапа.)*

### 6.5 Вторая итерация: эволюция работающего кода (финал программы)

Возврат к **уже закрытой** задаче с изменённым условием от наставника (объём данных вырос на порядки / ужесточилось требование к времени / сменился формат входа). Задача — **отрефакторить, не переписать с нуля**: понять собственный код через недели, опереться на тестовую инфраструктуру (suite держит изменение — рефакторинг безопасен), пересмотреть структуру там, где новое условие этого требует, а не залатать точечно. Эволюция работающего кода под новые требования — отдельная компетенция и основная форма работы инженера в индустрии: кода «с нуля» там мало, кода «под изменившиеся условия» — большинство. Конкретная задача и триггер изменения — от наставника по итогам этапов.

## 7. Журнал работы

### 2026-06-12
- Программа подготовки сформирована наставником под Windows-стек (адаптация общей программы курса; задачник — геометрия точек 3D) и **передана студентке в Telegram** вместе с материалами курса (00–03).
- Следующие шаги студентки: прочитать AGENTS.md целиком и материалы курса, согласовать с наставником темп (часы/нед с учётом диплома), поднять окружение (слои 1–3 из § 5), создать GitHub-репозиторий и расшарить на `auspehov@inobitec.com`, начать задачу 1 (§ 6.1). Вопросы — наставнику в Telegram.

### *(дальше — записи по ходу работы; формат: дата, что сделано, что не получилось, что заметила в работе с DeepSeek)*

### 2026-06-15
*Что сделано*
Вполнены шаги 1-3 из лестницы слоёв (параграф 5):
- native tools prompt не является "программой" - он выполняет vcvars64.bat, который обращается у другому .bat c флагом х64
  т.е. это консоль настроенными параметрами, типа переменных окружения

- сделана репо по структуре; .clang-format нужен для форматирования самих С/С++ файлов, но он сам по себе бесполезен
  к нему нужно доустановить программу clang-format, тогда оно будет сообщать о несоответствии формата

  .editorconfig - правила для восприятия разными редакторами. он применяется только для тех файлов, что лежат ниже него в иерархии
  root = true - не ищи editorconfig выше этого уровня
  
- сделан hello.cpp и запускается он не просто через cl а с множеством флагов (cl /std:c++latest /W4 /permissive- /EHsc /Od /Zi /MDd /fsanitize=address hello.cpp)

- получилось исправить старую проблему с error 403 на гите, т.к. в диспетчере учёта данных была настройка на старый аккаунт

*Что не получилось*: deepseek писал про кодировку CP1251, что не может её поставить в editconfigurator, потому он посоветовал latin1. 
Пока осталась latin1, нужно иметь ввиду что потенциально в ней может быть проблема с русским

*Что заметила в работе с DeepSeek*: 
1. в какой-то момент он добавлял что-то "от себя" - например про форматы файлов для .gitignore, он хотел добавить .ilk .exe ...
это не бред, так что наверное можно было этому и поверить
2. из критичного - один раз он переписал полностью hello.cpp хотя я ему не говорила этого делать

### 2026-06-16
*Что сделано*:
- упрощены .editorconfig и .clang-format до минимальных рабочих, т.к. было ощущение что в них есть лишнее
- если cpch1251, то setlocale(LC_ALL, "Russian_Russia.1251") ни на что не влияет 
- написана первая программа на с++ из параграфа 6, лежит в t1_dist_matrix
  собрала руками через cl /c ... + link /DEBUG /OUT...
- зауск через echo NUMBER | folder/main.cpp - этому мешали \r и пробелы. повяляются при вводе с echo
- common/exit_codes.hpp сделан заголовочник чтобы не было "магиических чисел"
- сделан "эталон" для тестов - ref.cpp
- тесты прогнаны руками (README.md, как прогнать тесты, (1), руками)
- переходный сценарий до make (build_and_test.bat в корне). в нём clang-format + compile + link + tets (для main и ref)

*Что не получилось*:
- перечитала место где про общение с deepseek, и получается что вчера я не задала ему верный контекст
  через 2 начальные реплики 
- осталась проблема с кодировкой. editorconfig не может в charset поставить cp1251 или ANSI
  даже если это написать, это ни на что не повлияет.
  пока что без cpch1251 в консоли русский текст отображаться не будет
- пока не до конца понятен смысл флагов: /EHsc /Od /MDd 
- в powershell .exe при запуске идёт сразу переход на новую команд. строку. 
  версия deepseek: powershell не показывает stderr. почему тогда на нормальных данных не выводится матрица? непонятно
- через FAR делать echo %ERRORLEVEL% невозможно. т.к. 
  echo %CMDCMDLINE%
  "C:\Windows\system32\cmd.exe" /s /c "echo %CMDCMDLINE%"
  по идее после каждой команды новый cmd. и тогда код возврата от моей программы
  теряется. и постоянно будет 0, т.к. это код внутреннего процесса в FAR
  работает только через Native Command Tools

*Что заметила при работе с DeepSeek*:
- как я поняла на первой задаче сборка уже с /cl (Сборка пока вручную: `cl /c` → `link`). 
  deepseek же при первом сообщении советовал сборку без /cl а ту, которая сразу выдаст .exe
  как будто он стремится к упрощению

- придумал факт, что объявлять переменные до цикла - это не идиоматичный С++, а из С89 
  правда, что я увидела это в курсе С99, но тут этого в "ловушках С++ как С" не было
  так что он просто придумал факт

- долго убеждал меня что с echo работает, когда на самом деле была проблема с /r и пробелами
  в итогде сделал dump и нашёл доп. символы

- при проблеме с Far и кодами возврата не стал углубляться в детали. предложил положиться на вывод
  cerr. по итогу да, проблема не в моей программе. но причину знать хотелось бы т.к. коды возвратов
  ещё должны встретиться и не раз

# 2026-06-17
*Что сделано*
- чтение Таненбаума ОС 1.1 и немного 1.2.3
- беседы с deepseek по ОС и несколько команд от него в PoweraShell
  наглядно, но по факту только теория
- конспект по всему этому

*Что не получилось*
- Weekly C++ видео. не совсем понятно что он объясняет, то есть
  сами слова понятны, но С++ контест у меня отсутствует, потому его объяснения 
  не совсем доносят смысл. надо будет выборочно посмотреть что-то другое
  из этих выпусков

*Что заметила при работе с DeepSeek*
- спросила по оглавлению что лучше почитать из Таненбаума. оглавление было в 3 страницы
  но он упал от такого количества текста. хотя это вроде немного
- перехваливал меня, мол я поняла и то, и это. 
  как будто старался быстрее сдвинуть меня дальше

# 2026-06-18 2026-06-19
*Что сделано*
- задачи по Си (чтение данных из строки в массив чисел; из чисел сделать форматную строку; 
  состояние партии в крестики-нолики (ничья, выигралиХ, выигралиО, партия продолжается))
- в целом, дописан конспект по теме 1 из базовых тем

*Что не получилось*
- cdb. оно пока не установлено, хотелось посмотреть системный вызов с её помощью
  пока откладывается
- очень по верхам прочитала про windows, архитектура довольно сложная по сравнению с linux
  возможно потом нужно будет вернуться к этой теме

*Что заметила при работе с DeepSeek*
- при чтении Таненбаума задавала ему вопросы. и он давал понятия, как бы
  заглядывая вперёд по контексту. это радует, потому что после этого было гораздо
  легче читать, чем при попытке понять термин с первого раза из самого контекста


# 2026-06-22
*Что сделано*
- задача 1 на Си
- сборка и тесты к ней через .bat

*Что не получилось*
- были проблемы с echo; | ... - почему-то оно не отлавливалось на fgets(...) == NULL
  и на с++ это echo; проходило, а на с - не всегда. по итогу решила type nul 
  сделать, это тоже пустой ввод
  возможно выбор в пользу type nul - от желания облегчить себе задачу,
  но с echo; получалось довольно много проверок, как будто это уже был костыль

*Что заметила при работе с DeepSeek*
- когда была проблема с echo; | ... он не сразу дал идею type nul 
  а сначала пытался добить с echo; 

# 2026-06-23
*Что сделано*
- c++ вторая задача, пока без тестов. завтра хочу вынести метод очистки
  строки от пробелов в общий метод, чтобы применять его во всех задачах

*Что не получилось*
- были проблемы с echo; | ... - почему-то оно не отлавливалось на fgets(...) == NULL
  и на с++ это echo; проходило, а на с - не всегда. по итогу решила type nul 
  сделать, это тоже пустой ввод
  возможно выбор в пользу type nul - от желания облегчить себе задачу,
  но с echo; получалось довольно много проверок, как будто это уже был костыль

*Что заметила при работе с DeepSeek*
- когда была проблема с echo; | ... он не сразу дал идею type nul 
  а сначала пытался добить с echo; 

# 2026-06-25
*Что сделано*
- начала конспект темы по powershell и cmd
- вынесла обрезку пробелов в common/string_utils.hpp, теперь этот метод используется и в main, и в ref
- цепочка тестов в t2_build_and_test.bat

*Что не получилось*
- изначально было желание ref и bat-ник полностью написать руками
  но немного обращалась к deepseek / предыдущим файлам за синтаксисом

*Что заметила при работе с DeepSeek*
- сегодня довольно мало его использовала, но в конце подметил неточности в коде от руки
  в целом, ничего замечено не было

# 2026-06-26 и 2026-06-28
*Что сделано*
- вторая задача на Си с тестами

*Что не получилось*
- в common\string_utils.h метод обрезки пробелов получился довольно запутанным
  возможно в будущем нужно будет его переписать

*Что заметила при работе с DeepSeek*
- в методе обрезки пробелов предалагал поменять многое в методе
  когда можно было изменить > на >= 
  возможно он хотел улучшить метод, т.к. он и остался довольно запутанным
  но я на это не согласилась

# 2026-06-30 и 2026-07-01
*Что сделано*
- c++ 3 задача
- gen_cloud к 3 задче
- power shell референс к 3 задаче

*Что не получилось*
- если в .bat сделать findstr /C:"Строка 1." build\err.txt > nul
  то слово "Строка" не будет нормально интерпретировано, т.к. findstr берёт аргументы не в chcp 1251,
  а в другой кодировке
  я не стала пытаться настраивать на рус. символы, а решила делать поиск строки " 1.", 
  т.к. номер всё равно надо как-то проверять

*Что заметила при работе с DeepSeek*
- при написании типа ref скрипта на powershell языке он не показал готовый код сразу
  а только начало, хотя по идее он знал что я никогда не писала на этом языке
  также, когда после этого я попросила выдавать как можно меньше готовых решений, показывать только синтаксис,
  который я спрошу, он так и делал

# 2026-07-01 и 2026-07-02
*Что сделано*
- задача 3 на Си
- сборка и тесты к ней в t3_bbox_c.bat

*Что не получилось*
- пока оставила проверку части строк без русских литералов
  из-за другой кодировки в findstr в батнике

*Что заметила при работе с DeepSeek*
- deepseek находил мелкие ошибки синтаксиса, которые я не замечала. это очень ускорядо исправления
- когда попросила прогнать тесты на моём коде, он кроме этого решил поправить код
  поправил он хорошо - я забыла сделать провеку при realloc на случай неудачи и free в некоторых местах
  но также он естественно и пересохранил файл, в другой кодировке. от этого слетели все выводы в out и err
  и комментарии  

# 2026-07-03 и 2026-07-04
*Что сделано*
- задача 4 на c++
- powershell референс к ней
- цепочка тестов в .bat
- полная проверка строки err.txt из .bat, через пересохранение err.txt в кодировке консоли Windows
  соотв., когда cmd отдаст findstr строку в ОЕМ, то и файл будет в той же кодировке
  и потом перекодировка в 1251 чтобы если тест упал, можно было прочитать ошибку в нормальной кодировке
- также сделана полная проверка строки к 2м версиям задачи 3 из .bat

*Что не получилось*
- deepseek сказал, что можно не пересохранять кодировки, если и тесты сделать poweshell скриптами
  мне захотелось так сделать, т.к. звучит логично. спросила у него про соответствие такого решения 
  нынешнему этапу проекта и правилам моего курса, он сказал, что это нарушит единообразие
  т.к. все прошлые тесты это .bat; довод про единообразие звучит логично, потому решила не делать ps1 для тестирования
  
*Что заметила при работе с DeepSeek*
- оказывается он долго неправильно открывал мои файлы (в utf-8), и говорил, что они неверно сохранены
  что символы слетели, и т.д. в ходе беседы я сказала, что файлы сохранены в asci1251 и тогда он сказал, 
  что это и правда так, а до этого просто не замечал
- очень хорошо заметил, что я зачем-то использую vector при фиксированном к-ве данных в точке, когда можно array

# 2026-07-06
*Что сделано*
- по совету deepseek в с++ №4 добавила проверку на конечность числа (не nan / inf)
  т.к. fromchars читает их как обычные числа
- в 3 и 4 задачах на Си в цикле while при чтении точек изменила while(!feof(stdin)) на while(1)
  т.к. чтение происходило внутри цикла, и проверка на конец будет только на след. итерации
  а прерывание с проверкой длины (при EOF моя функция делает её отрицательной) и так было внутри цикла
- также по совету deepseek в №3-4 на Си добавила проверку при первом malloc, а не только при realloc
- номер 4 на Си
- цепочка тестов к нему в bat файле

*Что не получилось*

*Что заметила при работе с DeepSeek*
- когда делала задачу №3 он посоветовал сделать while(!feof(stdin)), а сегодня он назвал это "антипаттерном"
- когда в конце попросила ещё раз найти моменты, которые нужно поправить, он придумал проверку, которой нет в С++
  версии, сказал что теперь в Си версии несостыковка с С++ версией. когда указала на это, он увидел, 
  что сказанного им не существует

# 2026-07-13
*Что сделано*
1. при сборках пограмм 1-2 на Си был флаг /EHsc хотя он предназначен для обработки ошибок на С++
   и во второй задаче забыла флаг /MDd, а для ASan он нужен. 
   
   у меня не было warning/error из-за /EHsc на .c или потерянного /MDd, хотя я понимаю, 
   что по идее они должны были быть

   и во второй задаче на Си я печатала строку, которая лежала в куче, котороую я до этого и освобождала
   было heap-use-after-free

2. добавила проверку и после cl и после link, т.к. в 4 задаче заметила ситуацию, 
   возникающую из-за старого obj

3. сравнения переделала на findstr, fc убрала в задачах 1-4

- задча 2. забыла #include <locale.h>, а было setlocale (LC_ALL...) который этого требует

- задача 4. 
    два раза было sizeof(points*) вместо sizeof(*points)/sizeof(struct Point)
	  то есть синтаксическая ошибка из-за непонятного * в конце 
	  выделение места делалось на размер указателя, который 8 байт. 
	  struct Point заниамет 8байт(double)*3 = 24байт - то есть того что выделялось явно не хватало

	  от компиляции до этого (видимо она была успешной) оставался obj 
	  проверки на успешность сборки на Си либо не было, либо она стояла после link.
	  link происходил со старым obj, поэтому даже с ошибками всё "собиралось".
	  попробовала удалить старый obj и link не произошёл, т.к. новый obj при cl не образовался


	  там же при реаллокации было realloc(points, capacity*sizeof(*points)) 
    как будто хочу положить points во "вместимость x размер points"
	  хотя по идее "вместимость x размер одного point". то есть места выделялось больше нужного

    для аргумента код 64, для данных в потоке - 65. было 64 для данных, исправила

# 2026-07-15
*Что сделано*
- общее: 

  вынесла struct Point в geometry.h и geometry.hpp

  чтение точек вынесла в string_utils.h и string_utils.hpp
    parse_point будет класть коорд в Point и возвр коды:
    0 - всё успешно
    1 - мало данных 
    2 - нечисловые данные
  main же в зависимости от этих кодов примет решение о выводе сообщ
  и упадёт с 65

- ...

- задача 4
  TODO:
  про чтение точек в память. не стала спрашивать deepseek об этом идейно, только по деталям реализации моей догадки
  кажется самое очевидное краткое - сделать за один цикл. тогда проблема только в том, что если точка проходит, 
  то выводиться она будет сразу. надо как-то задержать выходной буфер. deepseek сказал что такой способ есть:
  // в начале программы setvbuf(stdout, NULL, _IOFBF, 4096);
  // перед return - fflush(stdout);
  но он не очень надёжный. если выделить места меньше, чем в итоге туда сложится, то выведется не всё
  но т.к. это именно на краткость / ёмкость, то мне кажется этот подход можно использовать 

# 2026-07-16 - 18, 2026-07-20 - 21

*Что сделано*

- что нашла после копии репо в новую папку: 
  в t1...bat t2...bat не было создания папки build поэтому при сборке в чистом репозитории
  происходило "Системе не удается найти указанный путь."

  string_utils.h - не было прописано #include "geometry.h" хотя Point оттуда использовался

  в string_utils закомментировала метод trim_string, когда переделывала 3 задачу - там появился parse_point,
  а в нём - своя работа с пробелами в начале, конечные не важны. отдельный метод обрезки от пробелов там уже не нужен
  и я закомментировала trim_string. хотя он был во второй задаче. .bat для №3 прошёл без ошибок, а ко второй
  я bat и не перезапускала. про зависимость 2ой от trim_string я уже забыла

  локально проходило, т.к. я исправила эти моменты (кроме создания build в .bat, этого не заметила),
  а залила, видимо, недоделанную версию

- Задача 1 (матрица расстояний)

  если эталон полностью повторяет саму задачу, то это как сверять результат программы с её же прогоном.
  попросила deepseek переписать чтобы не было повторения main.c; то же самое с main.cpp и ref.cpp

  про хранение X Y в отдельных массивах. заменила на массив из Points

  константа pi: решила брать штатную и в Си, т.к. всё равно подключён math.h, и из него можно взять M_PI

- Задача 2 (паспорт фигуры)

  обрезка строки. С++ версия короче, т.к. в ней я не иду циклом, а использую first_of / last_not_of.
  в Си прямой аналог не нашла, но есть strspn, чтобы передвинуть указатель на длину пробельных символов в начале.
  схожего с last_not_of не нашла, поэтому обрезку последних пробелов оставила через цикл while

  вопрос про расхождение идиоматики. из того что погуглила, контейнер - это обёртка над коллекцией объектов.
  позволяет более удобно взаимодействовать с коллекцией, сам контроллирует память.
  контейнеров в с++ много (тот же std::vector который тоже у меня был) и std::string - один из них.
  в Си контейнеров нет, это видно на примере работы со строками. но эта разница будет не только при обработке строк,
  а в целом при работе с коллекиями. то есть то, что видно у меня на данном этапе при обработке строк,
  это частный случай расхождения идиоматики С/С++ в контейнерах

  разделение форм и индексов. завела массив с формами слов, и потом подставляю его в итоговую строку по индексу,
  и функция отдаёт индекс, а не строку

- Задача 3 (bbox и центроид)

  чтение и парс строки в Си. нашла sscanf - подходит sscanf("%lf %lf %lf", ...)
    но будет оставаться момент с тем, что неизвестен символ, на котором остановилось распознавание.
    поискала как распрсить по пробелу - strtok; в ней всегда один указатель на след. подстроку на всю программу.
    многопоточности у меня нет, но при использ. strtok() кидается warning,
    поэтому strtok_s - у неё нет проблем с одним static-указателем

  min / max / центроид через max() / lowest(). заменила на STL-идиоматичные вещи:
    через std::views::transform "смотрю" на .x, .y, .z всех объектов points;
    через std::ranges::max/min - поиск мин / макс по осям и ими же инициализация границ;
    центроид через std::reduce который по умолчанию складывает элементы.
    изначально у меня было также и ranges::to<vector>(), но это копирование,
    deepseek после проверки предложил сделать через views::transform - при вызове ranges 
    будет обращение к тому же points просто через определённую "призму"

  выделение памяти в vector в С++
    vector - динамич. массив, хранит ссылку на начало в куче, размер и вместимость.
    vector - контейнер, которому нужно выделение памяти. в контейнерах для этого есть allocator.
    он выделяет память, как только размер достигает вместимости.
    allocator есть в конструкторе контейнера, а по дефолту = std::allocator; 
    при выделении памяти std::allocator::allocate внутри allocate происходит _STD _Allocate<_New_alignof<_Ty>>(...)
    и после цепочки вызовов будет 
    ```
    ... _Allocate(const size_t _Bytes) {
        return ::operator new(_Bytes);
    }
    ```
    и потом также будет ::operator delete(...).
    то есть аллокатор - это умная обёртка над выделением и очисткой памяти, которая в Си делается руками.
    new / delete схожи с malloc / free, но есть отличия. как я поняла, new /... более безопасные
    и более "умные" чем malloc /...; для new /... память берётся из "Free Store" (некий С++ специфичный отдел памяти)
    для malloc/... - из кучи.
    
    откуда читала
    [https://stackoverflow.com/questions/52255327/how-are-allocator-in-c-implemented]
    [https://stackoverflow.com/questions/240212/what-is-the-difference-between-new-delete-and-malloc-free]
    [https://en.cppreference.com/cpp/memory/allocator]
    + deepseek подсказал куда искать внутри xmemory чтобы найти operator new

- Задача 4 (фильтр по радиусу)

  чтение точек в память.
  сначала обе версии сделала через отложенный вывод (в Си setvbuf и в С++ ostringstream, 
  т.к. deepseek посоветовал ostringstream как более идиоматичный). пришла к тому, что 
  в С++ версии вывод через filter + transform, а они работают с контейнерами. deepseek подобрал решение 
  и без vector, и без if, и идиоматичное, но получилось 
  ```
  for (const auto& s : std::views::single(p)
        | std::views::filter(should_keep)
        | std::views::transform(format)){out << s;}
  ```
  одна точка будет восприниматься как диапазон. мне кажется это странным, и deepseek потом называл это костылём.
  поэтому в Си оставлю без хранения, с задержкой выходного буфера,
  а в С++ буду хранить, чтобы потом сделать вывод через std::views 
  
  "что оставляем и что делаем":
    по тому что я поискала и спроисла ИИ, это идиома filter + transform

  комапактность задачи 4:
    как мне показалось, сокращение ядра - это сокращение ф-ции main

    С++: спросила deepseek про идиомы, которые можно применить для данной задачи.
    2 из них мне понравились: вынесение чтения точек в отдельную ф-цию и изменение сигнатуры 
    `parse_point на std::expected<Point, int>`. также, я решила и работу с радиусом вынести в ф-цию
    
    изначально хотела хранить счётчик и строку в глоб. памяти, чтобы печать в cerr была через main.
    оказалось, хранение м-у вызовами в глоб. памяти - это антипаттерн. 
    после сделала с передачей ссылок на переменные. в итоге в целях сокращения решила давать 
    get_points возможность печатать, т.к. deepseek сказал, что это уже ф-ция "оркестр", 
    работает с IO, у неё много функционала. + она локальная, а не в common\...

    Си: ядро сократилось после решения не хранить точки. также, в целях сокращения вынесла get_points.
    он будет и читать, и печатать. из main только возврат кода, если была ошибка.
    можно и работу с радиусом вынести, но main и так укоротился. мне кажется лучше оставить 
    работу с argc, argv в main


*Что не получилось*
- заметила что на гите русские комментарии отражаются в другой кодировке
  нужно будет посмотреть, как это исправить
- deepseek сказал, что strcpy_s - привязан к MSVC-специфичный. но раз у меня сборка под Windows, 
  то мне не кажется что в этом есть проблема
- ради сокрашения отдала get_points() печать ошибок, а в Си и печать точек
  хотя не хотелось бы этого делать. но как ещё сократить main, не отдав печать другой ф-ции, я не придумала


# 2026-07-23
*Что сделано*
- убрала референсы из удалённого репозитория через git rm --cached ... и добавила их в .gitignore
- прогон батников и errorlevel / %errorlevel%.
  errorlevel - уровень ошибки последней выполненной программы;
  %errorlevel% - переменная среды. если не была определена то на её место подставится значение cmd-шного errorlevel. 
  если между if errorlevel ... и echo %errorlevel% выполнилось что-то ещё, то %errorlevel% поменяется

  если после успешно выполненных bat-ников в консоли сделать echo %errorlevel%, то будет тот код ошибки, 
  который был в последнем тесте в bat-нике

  если запустить через скрипт (я сделала через ps1) то он внутри себя запустит доп. процесс cmd.exe, и при условии что всё отработает
  корректно, то последним кодом будет 0. потому что это код завершения процесса cmd.exe

  если же будет ошибка и выход с exit /b 1 (который даёт код завершения cmd), то будет выведен код 1. потому что в этом случае это и есть 
  код завершения процесса cmd

  процессы консолей увидела через: for /l %i in () do @(cls & tasklist /fi "imagename eq cmd.exe" & timeout /t 1 >nul)

  как только запускается .ps1 (.\t1_build_and_test.bat $LASTEXITCODE), то количество консолей вырастает на одну 
  
# 2026-07-24
*Что сделано*
- setlocale в main.cpp t2-t3

  на что влияет setlocale - на Си-шные locale-зависимые функции, которые работают с символами (сравнивают символы, делают их toupper, tolower, смотрят какая буква старше, и влияют на вывод цифр).

  t2: из string_utils используются trim + is_empty - там только с++ функции, не из си-шных локаль-зависимых
  поэтому когда я попробовала убрать setlocale из второй, то всё продолжило работать без проблем. и вывод через cout - setloc. не повлияет. без setlocale t2 бы не работала, если бы там была какая-то работа со строкой на русском (типа "сделать все буквы называния строчными")

  t3: из си локаль-зависимых ф-ций только std::isspace(...). но он будет также работать на рус. пробелах/табах/возвр каретки/переносах. вывод через std::print, deepseek подтвердил что это C++23 ф-ция, не Си-шная

  то есть получается, setlocale в данных задачах не выполняла никакой функции

  эксперимент с setlocale + printf + strtod (a = 3.15, b = strtod("3.14") и потом ещё раз после setlocale... b = strtod...). результат:
  ```
  BEFORE LOCALE
  a = 3.150
  b = 3.140
  AFTER LOCALE
  a = 3,150
  b = 3,000
  ```
  точка сменилась на запятую при выводе, и если вводить с точкой, то указатель конца распознавания покажет точку. если ввести с запятой - всё ок.
  оригинальная же локаль наоборт не принимает запятую, но зато принимает точку.

  если в программе, которая принимает числа вида "3.14" сделать  setlocale(LC_ALL, "Russian_Russia.1251"); то будут теряться дробные части. чтобы это исправить, мне кажется, нужно исключить влияние на LC_NUMERIC, а LC_ALL влияет на все макро константы. то есть надо поменять LC_ALL на LC_CTYPE - влияние только на символы (ну и возможно добавить setlocale с LC_TIME/MONETARY если в задаче есть работа с этими вещами). попробовала поменять в тестовом файле, получилось:
  ```
  BEFORE LOCALE
  a = 3.150
  b = 3.140
  AFTER LOCALE
  a = 3.150
  b = 3.140
  ```

- сборка геометрического ядра в отдельный модуль. 
  вынесла struct Point и parse_point в geometry и теперь в geometry.h - объявления, в geometry.c - реализация. с этим уходит include geometry.h из string_utils.h
  то же самое со string_utils - разделение на .h и .c; вместе с этим уходят static inline - т.к. теперь это будет отдельный модуль, а не static ф-ции, инкапсулированные для каждого файла

# 2026-07-25
*Что сделано*
- раздельная сборка. также сделала для С++ части проекта; аналогичные действия, но в hpp также влкючила string / string_view / expected, 
  т.к. это не стандартные типы std

  при раздельной сборке попутно образовывются только новые .obj от str._utils и geom. - они и так уйдут в .gitignore

- оформление модуля как статической библиотеки и линковка с t3-t4
  common\string_utils....obj + common\geometry....obj складываются в common\lib_...lib, на этапе линковки main линкуется только с common\lib_...lib

  на этом этапе новый файл - .lib; каждый раз появляется от объединения .obj - значит и lib уходит в .gitignore

# 2026-07-26
*Что сделано*
- сборка динамической библиотеки и линковка через .lib с потребителем (C++ часть проекта)

  на этом этапе будет также образовываться .pdb, а ещё - .lib как таблица ссылок и .exp - таблица экспорта для разрешения циклич. зависимостей.
  они оба будут возникать на этапе сборки .dll, так что их тоже можно положить в .gitignore.

- сборка динамической библиотеки и линковка через .lib с потребителем (C часть проекта)

# 2026-07-28

*Что сделано*
- размещение common.dll чтобы была только одина копия. сейчас с dll 4 задачи, поэтому 4 копии.
  сначала была идея класть все артефакты в build и называть единообразно. 
  будет перезапись и не будет мн-во артефактов. но deepseek посоветовал так не делать, т.к это вызовет смешение и проблемы на этапе с Make
  
  пока будет один common.dll, будет уходить в build. через set "PATH=%~dp0/build;%PATH%" где %~dp0 - путь до bat-ника. т.к. абсолютный путь как будто нехорошо - при переносе проекта на др. комп. могут быть конфликты

  спросила, куда лучше класть common.dll - в build или в common, deepseek сказал что common - каталог исходников. получается, я неправильно делаю, что туда же кидаю .obj, .lib? и вообще все артефакты должны идти в build? тогда так по идее, и deepseek подтвердил.

  после единого common.dll немного пререстрою структуру - артефакты будут идти в build, который будет иметь поддиректории для каждой задачи. и dll тогда будет разделаться на _c и _cpp (т.к. deepseek сказал, что для make стоит делать именно так); с решением перестроить он тоже согласился, так что доводов против этого решения нет

- exit-коды. во всех .bat, кроме t3...bat (там последний код и должен быть 0), последним идёт тест, в котором есть какой-то код ошибки.
  (t1 - 64, t2 - 65, t4 - 1 т.к. там тест с одной непроходящей точкой и regex ищет хоть какой-то символ, не находит, findstr отдаёт 1).
  значит, при запуске обёртки не всегда будет запущен новый cmd.exe, deepseek подтвердил. тогда проще всего последней операцией делать exit /b 0, 
  т.к. если все тесты прошли, это и означает код выхода 0. при ошибке у меня всегда exit /b 1, так что при выходе с ошибкой errorlevel должен быть 1

- setlocale в t2 на Си. поставила LC_NUMERIC - результат не изменился. как и с LC_CTYPE.
  на какие ф-ции в t2 setlocale влияет: isspace, printf, fprintf.

  т.к. кодировка букв ставится из bat, то потенциально могло влиять на разделитель - запятая вместо точки в выводе. но кол-во вершин - целое число, нет разделителей. если в задаче прибавится условие, например, что просле имени и к-ва вершин будут вводиться координаты вершины (а они уже явно double), то это повлияло бы на восприятие чисел с точкой - появилась бы strtod, на которую setlocale влияет.
  на isspace на самом деле не влияет, т.к. русские пробельные символы все стандартные.

  setlocale в этой задаче с разными макроконстантами не влияет на результат - кодировка устанавливается из консоли; программа никак не трогает строку, ничего в ней не сравнивает; число вершин - всегда целое. если будет изменение задачи, то нужно будет с LC_CTYPE, но сейчас setlocale ни на что не влияет. раз ни на что не влияет - убриаю

- перевела конспекты на UTF-8. в .editorconfig За это отвечает параметр charset = ...; но editorconfig не поддерживает ANSI и её страницы, потому
  что это платформенно-специфичная вещь, т.к. ANSI - от windows. в editorconfig невозможно прописать charset=cp1251, поэтому оставила данный параметр не заданным

*Что не получилось*
- заметила интересный вывод в main_out.txt у второй задачи: Фигура <звезда>: 6 вершин. то есть уехали кавычки. это происходит из-за перекодировки
  которую я сама делаю для совместимости с findstr

*Что заметила при работе с deepseek*
- когда хотела разместить все артефакты в build, он активно был против этого. говорил что это будет конфликтовать с make который будет дальше. пока
  я не понимаю что это, поэтому поверила ему и не буду смешивать все артефакты и перезаписывать их. но также он настаивал, что класть common.dll по разным папкам, как сейчас, - это хорошо, иначе будет конфликт при вызове. когда сказала, что это пересборка одной и той же программы, он сказал, что был не прав и излишне обобщил.  

# 2026-07-29
*Что сделано*
- делала тестирование проекта, склонировала в другую папку. в осн. папке было уже много изменений, а из тестовой папки заметила что у меня 
  mkdir build Находится после PATH=%~dp0/build... конечно был конфликт из-за этого. перенесла mkdir выше и закоммитила из тест. папки.
  это привело к конфликту, потому что в осн. папке этого коммита не будет. не подумала про это. по итогу просто решилось через git pull - он 
  принёс только изменения и надо было убрать ненужное в файле, и потом ещё раз git add ... git commit ...

# 2026-07-30
*Что сделано*
- доделала конспект по теме "Файловые системы ..."

# 2026-07-31
*Что сделано*
- пока буду делать make для t1 c/c++, и будут только проверки кодов. проверка строк пока откладывается
- winget install ezwinports.make, make --version: 4.4.1

# 2026-08-10 - 2026-08-12
*Что сделано*
- минимальная версия makefile - переменные для windows, переменные целей, phony-цели. и правила для сборок .ехе первой задачи 
  и одного теста для cpp первой задачи

*Что не получилось*
- плохо вышло с коммитами. я думала довести makefile до приемлемого состояния хотя бы для 1ой задачи - все тесты, в т.ч. строковые.
  но произошло так, что в конце дня когда я прогоняла цель test то возникла ситуация "отказано в доступе" при выполнеии run_case.exe; 
  deepseek помог найти причину - этот exe весил 0 байт. я даже примерно не понимаю, каким образом это вышло. потому что я не перезаписывала .exe; deepseek посоветовал более регулярно коммитить, и сказал, что если были бы закоммичены изменения, последовательно, то причину было бы возможно найти

*Что заметила при работе с deepseek*
- в makefile в разделе зависимостей deepseek предложил сделать 
  ```
  $(BINDIR)/%.obj: %.cpp 
    $(CXX) ...
  ```
  т.е. на моменте, когда цель ещё не существует, будет непонятно, куда её класть в итоге компиляции.
  пререквизит тут невозможен, потому что нет конкретной папки, которую можно прописать в пререкв. после | для проверки существования.
  это разрешается добавлением `$(MKDIR) $(dir $@)`. но из этого тоже проблемы, но другие

- предалагает `$(BINDIR)/t1_dist_matrix_cpp` вместо `$(BINDIR)\t1_dist_matrix_cpp`, говорит что это работает. хотя на прямой слеш -
  "ошибка в синтаксисе команды" в cmd. но если обратный слеш не окружён с 2 сторон символами, то make начинает воспринимать его, как спецсимвол

- `$(BINDIR)/%.obj: %.cpp` (line95) + `$(BINDIR)/%.obj: %.c` (line100) - то что изначально deepseek предложил. это приводило к:
  ```
  Makefile:100: warning: overriding recipe for target 'build\debug%.obj'
  Makefile:95: warning: ignoring old recipe for target 'build\debug%.obj'
  make: *** No rule to make target 'build\debug\t1_dist_matrix_cpp\main.obj', needed by 'build\debug\t1_dist_matrix_cpp\main.exe'.  Stop.
  ```
  пока без совета deepseek. как я поняла, make не нравится, т.к. для него это выглядит как записывание новых obj поверх деланных на стр.95.
  но имена-то у них разные; на практике я не понимаю что именно с чем он путает. надо задать deepseek вопрос про это, но не прямой

  пока что, попробовала разные вариации, как вылечить warning-и (через абсолют. путь)

  вариант 1 - make 2 раза "делает" одинак. папку (по итогу, он НЕ делает её 2 раза).
  внутри make:
  ```
  $(BINDIR)\t1_dist_matrix_cpp\main.obj: t1_dist_matrix_cpp\main.cpp 
	  $(MKDIR) $(dir $@)
	  $(CXX) /c /Fo:$@ $(CXXFLAGS) $<   
  ...
  # линковка. для первой задачи и модуля geometry линковка статическая, тк тут один geometry без string_utils
  $(BINDIR)\t1_dist_matrix_cpp\main.exe: $(T1_CPP_OBJ) | $(BINDIR)\t1_dist_matrix_cpp
  ...
  ```
  что выводит make -n all:
  ```
  mkdir build\debug\t1_dist_matrix_cpp\
  cl /c ...
  mkdir build\debug\common
  cl /c ...
  mkdir build\debug\t1_dist_matrix_cpp
  link ...
  то же с Си-версией - 2папки
  ```
  то есть, видимо в момент рецепта объектника - mkdir, и в момент линковки ..../t1_dist_matrix_cpp - пререкв. и make делает его
  ещё раз. но почему он не видит папку, которую только что сделал? 

  после беседы с deepseek: он рассказал, что make не может overwrite папку просто так - это выродилось бы в ошибку - "Подпапка или файл ... уже существует". повторного создания папки нет. просто на этапе построения дерева инструкций make видит "...\t1_dist_matrix_cpp\" и "...\t1_dist_matrix_cpp" как 2 разные папки, поэтому если сделать make -n all, то он покажет mkdir для них обоих. 
  на самом же деле, во второй раз, при линковке, там order-only - т.е. проверка существования - и в этот момент папка же будет и mkdir для неё не произойдёт. make -n не моделирует поведение рецептов - то есть он не знает, что какой-то рецепт до этого создал по сути такую же папку. поэтому будут выводиться потенциально лишние команды

  то, что у меня получилось и сработало, сработало чисто ввиду порядка команд. сначала жёсткий mkdir в рецепте, потом order-only -> mkdir (который увидел что папка есть и не стал создавать повторно). если наоборот - то будет ошибка от повторного создания папки. т.е. то, что у меня получлось, опирается на линейный ход команд. нельзя так делать, т.к. make не гарантирует порядка выполнения команд

  вариант 2. убрать один mkdir.
  если вообще убрать `$(MKDIR) $(dir $@)` - нет дублей папок, но плохой порядок - из-за него файл кладётся в несущ-в папку, потому что папка делается позже, на этапе линковки. а объектник лежит "нигде"
  ```
  cl /c /Fo:build\debug\t1_dist_matrix_cpp\main.obj ...
  mkdir build\debug\common
  cl /c ...
  mkdir build\debug\t1_dist_matrix_cpp
  ```
  вариант 3 order-only. вариант 2 лечится order-only пререквизитом:
  ```
  $(BINDIR)\t1_dist_matrix_cpp\main.obj: t1_dist_matrix_cpp\main.cpp | $(BINDIR)\t1_dist_matrix_cpp
  ```
  так, всё делается в верном порядке и не дублируется. но это всё ещё абслютные пути для каждого файла - много дублирования

  попросила deepseek навести на мысть, почему были проблемы от $(BINDIR)\%.obj:... оказалось, что \ перед специсмиволом экранирует его.
  и получается буквально build\debug%.obj с % как литералом. stam не работает, совпадения не ищутся, obj-цели не делаются.

  исправлено на $(BINDIR)\\%.obj: - экранирование обр. слеша. но если есть вот такой паттерн - то order-only не сработают, им нужны конкретные
  имена. без order-only будет та же проблема с "двойным" созданием папок и зависимостью от порядка команд. deepseek предлагает сделать `-$(MKDIR) $(dir $@)` в каждом нужном рецепте. -префикс для игнор. ошибок. потом он упоминает, что это плохо, но в этот моменте "так делают". тогда раздел с 
  рецептом для создания папкой просто уберётся  

  в wildcard слеш должен быть прямым. в остальных путях - обратным. если перед %, то экранированным.

# 2026-08-13
*Что сделано*
- (из вчерашнего, вывод) - рецепт должен раскрыться в команду полностью, иначе в нём есть ошибка. make не может "прятать" какой-то его компонент.
  если был рецепт типа `$(RUN_CASE) "echo abc ..." 65 && $(TOUCH) $@`, а при печати в cmd он раскрылся как
  `"echo abc ..." 65 && type nul > build\debug\test\t1_cpp_abc.ok`, то RUN_CASE не раскрылся, в переменной ошибка.

- дописаны тесты на exit-коды для остальных случаев (для Си и С++ частей)

- вынесла exit-коды в отдельные переменные, чтобы не было "магических чисел"

- тесты на строки, пока только с числами, т.к. t1, через findstr.
  из замеченного: при ошибке в рецепте make будет возвращать код ошибки, с которым упала команда.
  например, если было `findstr /C: "..."` (лишний пробел), то будет: `make: *** [Makefile:...] Error 2`. если же сделать `findstr`, и такая строка не будет найдена, то будет: `make: *** [Makefile:...] Error 1`

- most vexing parse - когда с++ компилятор читает создание объекта, но воспринимает его как декларацию функции. т.к. по правилам с++ любая 
  неоднозначная строка, которая м.б. воспринята как функция, будет воспринята как функция. у меня это получилось от
  ```
  std::string res_string(std::istreambuf_iterator<char>(res_stream), std::istreambuf_iterator<char>());
  ```
  но без MVP вот такое `A f();` было бы воспринято как "переменная t типа А, конструктор по умолчанию" - т.е. как объявл. переменной, не ф-ции.

  - написан test/check_contains.cpp - проверяет, что целевой файл содержит подстроку из файла-ожидания. там же была ситуация, - deepseek
  посоветовал сделать io_fail exit code если файл не удалось открыть. потом у меня мысль "во всех вспомогат. .cpp сделать дифферинциацию кодов"

*Что не получилось*
- заметила что 
  ```
  $(BINDIR)\\%.obj: %.cpp | $(CPP_DIRS)
	  $(CXX) /c /Fo:$@ $(CXXFLAGS) $< 
  ```
  не учитывает что у задачи 1 должен быть /DCOMMON_STATIC, т.е. там помечаются ф-ции в main как import. Но там только ссылка на geometry.hpp;
  поэтомтоу проходит незаметно и всё собирается даже без флага, хотя он нужен. надо будет разнести сборку объектников, насколько универсально как сейчас не получается сделать

*Что заметила при работе с deepseek*
- сначала предложил сделать код io_fail при открытии во вспомогат. .exe без потребителей этого кода, потом сказал, что разные коды нужны, 
  когда есть потребитель. поэтому я решила во всех служебных задачах код любой ошибки = 1. мне кажется наиболее логично и для единообразция

# 2026-08-14 
*Что сделано*
- придумала решение проблемы с флагом во время компиляции, примерно: `$(T1_C_EXE): /tab COMMON_FLAG := /DCOMMON_STATIC`. deepseek подкорректировал 
  синтаксис (без /tab), рассказал, что это существует как отдельная категория, не пререквизит, после цели. это target-specific variable, будет распространяться на все правила в под-целях главной цели

- написана сборка для string_utils статическая; динамическая для geometry и string_utils; 
  сборка утилиты для проверки содержания подстроки в подаваемом файле; и компиляция, линковка второй задачи с++; тест на файле (на вход - файл tests\input_data\t2_input, выход - в $(BINDIR)\t2_passport_cpp\t2_res, сравнение с tests\expect\t2_norm) - что файл-выход на нормальных данных равен ожидаемому результату

*Что не получилось*
- надо будет дописать 1) до конца тесты ко второй с++; 2) сборку второй Си; 3) полные тесты ко второй Си

*Что заматеила при работе с deepseek*
- по вопросам по make даёт ссылки на gnu make manual - удобно, что сразу даётся нужный раздел документации. так же иногда даёт и на cpp reference

# 2026-08-15
*Что сделано*
- тесты на exit-коды 2 задача (C++ часть) и файлы входных данных
- доделала тестовую цепочку для Си-части второй задачи
- исправила дефект в string_utils.c get_string(), который до этого был незаметен. get_string возвращал EOF-ошибку, как только видел символ EOF 
  (даже если до этого в строке были символы). метод не смотрел, чтобы при этом и длина строки была == 0.
  проходило незаметно из-за ввода через echo - он добавлял \n в конце строки всегда и цикл останавливался именно на \n, не доходя до EOF.

*Что не получилось*

*Что заматеила при работе с deepseek*
- решила делать тесты через подачу файлов, а не echo. я не учла, что .ok файлы при этом должны зависеть от своих входных данных и эталона 
  (если он есть). т.к. если вход. данные / эталон поменялись, то тест надо прогнать ещё раз. deepseek указал на этот момент

- помог найти причину дефекта с EOF в string_utils.c get_string()

# 2026-08-17 2026-08-18
*Что сделано*
- придумала как сделать сборку t3 main.exe с учётом что .lib не будет целью (цель именно .dll) - в пререкв. main.exe поставить .dll, 
  а в рецепте использовать .lib Т.к. он уже будет продуктом .dll; deepseek подкорректировал - сказал, что при этом lib не включён в граф,
  и если удалить .lib а .dll оставить, то make упадёт с тем, что не удаётся открыть ...lib; пришла к multi-target а потом - к grouped targets
  для большей логичности

- путь до .dll в makefile делается не через %~dp0, а через специфичную для make переменную CURDIR - полный путь до текущей диркетории

- тестовые данные (на вход / выход) и тестовая цепочка для 3 задачи на С и С++

- deepseek напомнил, что по 4.9 п.1 переходные сценарии в виде .bat удаляются, поэтому удалила .bat к t1-t3

- исправила вывод строки для отриц. радиуса для t4 C - было "Получено: %.3f\n", r" (после приведения радиуса к double), 
  стало "Получено: %s\n", argv[1]"

*Что не получилось*
- пока в моменте запуска main.exe t3-t4 будет исп. set "PATH=$(CURDIR)\$(BINDIR)\common;%PATH%" - то есть запуск make строго из корня, не из любой 
  подпапки. но так пока во всём make, в т.ч. поэтому решила не переделывать на то, чтобы вычилять корень из makefile и перезапускаться оттуда.
  пока в этом нет нужды

*Что заматеила при работе с deepseek*
- придумала как сделать сборку t3 main.exe с учётом что .lib не будет целью (цель именно .dll) - в пререкв. main.exe поставить .dll, 
  а в рецепте использовать .lib Т.к. он уже будет продуктом .dll; deepseek подкорректировал - сказал, что при этом lib не включён в граф,
  и если удалить .lib а .dll оставить, то make упадёт с тем, что не удаётся открыть ...lib; предложил multi-target - .dll .lib

  спросила, явл. ли это синтакс. сахаром? да, это так. и deepseek рассказал, что в случае с классическим multi-target 
  с .lib .dll происходит раскрытие в два правила. и когда нет
  ни lib ни dll, то рецепт выполнится два раза. файлы сойдутся, т.к. это будут те же файлы, но для файлового дерева это не совсем честно.
  deepseek показал что так "dll lib &: ..." - grouped targets - одна команда создаёт 2файла. так не будет пересоздавания без нужды.

  продолжила спрашивать, т.к. мне показалось странным - например, lib будет создаваться второй раз, даже если был выполнен рецепт в .dll
  и он уже есть? как будто так не дожно быть - он будет существоввать и будет свежим. по итогу deepseek подтвердил это. потом я увидела, что multi-target вида `...dll ...lib: ... \tab lib .../OUT:$@` раскроется на команды, в одной из которых будет link с OUT в .lib, хотя это принципиально неверно. для link целью должна быть именно .dll; итог - grouped target (чтобы избежать ситуации линка в .lib)
  и жёсткий путь OUT:..dll (т.к. deepseek сказал, что $@ с grouped target Будет давать ту цель, которая триггернула правило. видимо оттого, что цели в "философии" make равноправны.) опять же для избежания линка в .lib, но удержания его в дереве, чтобы при его удалении была пересборка

- нашёл достаточно много неточностей - забыла h/hpp в зависимостях, мёртвый код, забыла изменить пререкв. с dll на lib в месте, про которое был
  прошлый пункт. и напомнил про удаление переходного сценария

- настаивал на .ps1 и gen_cloud в makefile, хотя у меня сейчас нет референсов и массового входа

- deepseek заметил расхождение в t4 C при выводе - выводилось не "...Получено: -10", а "... Получено: -10.000". проходило из-за того, что происходит
  поиск подстроки через check_contains; но и через findstr получается была такая же проблема

# 2026-08-19
*Что сделано*
- теперь тестовая цепочка первой задачи сообразна остальным: на вход файл, на выходе файл. нет echo и findstr
- RUN_CASE -> RUN_CASE_CMD, FIND_SUBSTR -> FIND_SUBSTR_CMD. отдельный блок ifeq ($(OS), Windows_NT)... для корректных путей в этих .exe
- пути переделаны на прямые слеши. для виндоус - приём с оборачиванием частей рецепта в WCMD = $(subst /,\,$1) а на линукс WCMD = $1
- флаги (/DLL, /c, COMMON_STATIC, ...) вынесены в платформенно-специфичные переменные
- SET_DLL_PATH теперь также платформаенно-специфичен
- флаг COMMON_FLAG теперь для t1-2: COMMON_FLAG := $(DEFINE)COMMON_STATIC для t3-4 - пустой. так, при работе паттерн-правил для объектников
  не будет оставаться висячего /D или -D при компиляции t3-4; 
  т.к. до этого было t1-2: COMMON_FLAG := COMMON_STATIC, t3-4 - также пусто. а в секции паттерн-правил было: 
  ```
  $(BINDIR)/%.obj: %.cpp | $(CPP_DIRS)
	  $(CXX) $(COMPILE_ONLY) $(DEFINE)$(COMMON_FLAG) $(OBJ_OUT)$@ $(CXXFLAGS) $<   
  ```
  и из-за $(DEFINE) в начале был висячий флаг при пустом COMMON_FLAG
- в заголовочниках geometry... string_utils... вся усл. компиялция (ifdef..) обёрнута в ещё один слой усл. компиляции - ifdef _WIN32, т.к. 
  все пометки ф-ций через __declspec... import/export нужны только под виндой. под линуксом в .so все будут экспортируемыми и g++ будет искать
  импортируемые для исп. файла в той же .so

*что не получилось*
(что нужно доделать)
- debug / release блок для линукс
- расширения вынести в платформенно-зависимые переменные
- оформить момент того, что на линукс не будет .lib от .dll, будет только .so (сам .dll) - ifeq блок т.к. тут дело не только в расширениях.
  для самой библиотеки завести переменные
  ```
  # Windows                       # Linux
  CPP_SHARED := common_cpp.dll    CPP_SHARED := libcommon_cpp.so
  C_SHARED   := common_c.dll      C_SHARED   := libcommon_c.so
  ```
- Header-зависимости (параграф 5 п.15 последний раздел, для линукса)
- проверить корректность на виртуалке
- run_case_cmd find_substr_cmd - на линукс возможен конфликт т.к. там .exe в конце. иметь ввиду этот момент
- fc/diff. diff может быть чувствителен к CRLF, возможно нужно будет сделать тесты более универсальными
от deepseek:
- run_case + exit-коды на Linux. ФАКТ, ВЫСОКИЙ std::system на POSIX возвращает не код выхода, а wait-status: реальный код сдвинут в старшие 8 бит (код << 8). Все ~40 exit-тестов на Linux будут «не совпало», даже при верной программе. Правится в tests/run_case.cpp через #ifdef _WIN32 (или WEXITSTATUS).
- std::print в C++23 на GCC. ПРЕДПОЛОЖЕНИЕ, СРЕДНИЙ Требуется GCC 14+ (и std::expected — GCC 12+). На виртуалке стоит проверить g++ --version, не гадать.

# 2026-08-20
*Что сделано*
- заполнен раздел флагов под линукс, в т.ч. debug/release. флаги не 1в1 с виндоус, но примерно. например, пока при release в link_flags 
  нет аналога /OPT:REF /OPT:ICF из виндоус
- расширения сделаны платформенно-зависимыми
- добавлен PIC_FLAGS. флаг -fPIC для объектников - под линуксом это нужно для позиционной независимости кода. под виндой это пустой флаг
- т.к. под линукс объектники   geometry... string_utils... теперь не разделяются (нет флагов компиляции), то блок с особыми _dll.obj нужен только
  под виндой. поэтому этот блок обёрнут в ifeq os Windows_NT ...
- линковка динамической библиотеки разнесена по блокам. т.к. под винду остаётся .lib в целях, а под линуксом такого "продукта линковки" не будет
- линковка задач 3-4 с dll переделана с учётом того, что под винду линковка будет с .lib, под линукс - с самой .so
- линукс версии динамич. библиотек - с преффиксом lib (deepseek посоветовал так сделать, т.к. это некая конвенция на линуксе)
- в run_case.cpp добавлена усл. компиляция - под линуксом exit-code будет не таким как под виндоус, и надо взять опред. байты, чтобы это был
  нужный код выхода процесса (WEXITSTATUS(status))

- посмотрела свои эталоны и результаты программ в hex-режиме. там везде CRLF а не просто LF. потому что было перенаправление crt stdout потока,
  а он по умолчанию текстовый. по итогу рассуждений с deepseek вывод такой: при тестировании на нормальных данных выход и эталон сравнивается
  полностью. при ошибочных случаях строка/строки эталона ищутся в выходном файле

*Что не получилось*
- это ПРЕДПОЛОЖЕНИЕ, СРЕДНИЙ от deepseek. пока не буду ничего делать по этому поводу, просто зафиксирую.
  ```
  Находка 4 — у .so нет -soname, DT_NEEDED привязан к пути запуска ПРЕДПОЛОЖЕНИЕ, СРЕДНИЙ
  .so линкуется без -Wl,-soname (греп подтвердил: soname/rpath нет нигде). При линковке exe с .so по полному пути линковщик пишет в DT_NEEDED этот путь. Т.е. exe «зашит» на build/debug/common/libcommon_cpp.so относительно CWD — поэтому и работает только из корня. SET_DLL_PATH/LD_LIBRARY_PATH для такого DT_NEEDED фактически не консультируется (записи со слэшем загрузчик берёт напрямую). Это глубже текущего этапа, но объясняет, почему «работает из корня» — не LD_LIBRARY_PATH, а CWD. Зафиксируй как известное; правится -Wl,-soname,libcommon_cpp.so + -Wl,-rpath — на этапе CMake.
  ```

- входные данные - с CRLF, на линукс это будет работать из-за trim-ов и isspace-проверок. то есть держится всё на парсере. 
  deepseek говорит что это скрытая зависимость: "смена парсера (например, ручное сравнение символов) молча сломает Linux. Плюс единообразие политики: если эталоны нормализуем в LF, то и входы логично хранить в LF — тогда обе платформы читают одинаковые байты, и ничто не зависит от терпимости к \r."

*Что заметила при работе с deepseek*
- makefile был уже "готов" - зеленые прогоны и все моменты по кросплатформ. сделаны (пока без проверки на практике).
  попросила "найти ещё один" в makefile. нашёл недочёты, - хардкод-расширение а не через $(); CRLF в тестовом файле - это бы уронило diff на линуксе; и RUN_CASE_CMD FIND_SUBSTR_CMD в пререквизитах t2 t4 - там слеши в др. сторону, но тесты собирались начиная с t1 и там был 
  нормальный RUN_CASE FIND_SUBSTR, который собирался раньше. но при распараллеливании от этого были бы проблемы

# 2026-08-21
*Что сделано*
- check_contains.cpp переписан, теперь это check.cpp с двумя флагами - `--equals` - равенство файлов, `--contains` - содержание f2 в f1
- makefile изменён в соотв. с новым check, а FC := fc / diff убран за ненадобностью
- в tests/expect tests/input_data CRLF -> LF, чтобы при переходе на лиукс не было проблем. через powershell-скрипт
  ```
  $p='desktop\inobitec_stud\tests\input_data\t4_test2'; $b=[IO.File]::ReadAllBytes($p); $o=[Collections.Generic.List[byte]]::new(); for($i=0;$i -lt $b.Length;$i++){ if($b[$i]-eq 13 -and $i+1 -lt $b.Length -and $b[$i+1]-eq 10){continue}; $o.Add($b[$i]) }; [IO.File]::WriteAllBytes($p,$o.ToArray()); "{0}: убрано CRLF: {1}" -f $p, ($b.Length - $o.Count)
  ```
  если в будущем сделать git merge, то LF заменятся CRLF

- включение в t1 Си MATH_PI т.к. _USE_MATH_DEFINES - msvc-специифчно
- t2-t4 замена слешей на прямые в include
- geometry.c parse_point переписан т.к. strcpy_s и strtok_s - MSVC-специфично

*Что не получилось*
- от deepseek: фиксация в журнале уже известного ограничения: .so без -soname/-rpath (DT_NEEDED привязан к пути запуска — работает только из корня) — правится на этапе CMake. пока не понятно что это, значит решится на след. этапе

- в geometry.c parse_point случай "больше 3х точек" будет возвращать 1, тогда как 1 - это мало точек. пока у меня такого теста нет, но возможно
  надо будет это исправить


# 2026-08-25
*Что сделано*
- добавила double pad перед x в common/geometry.hpp. make test -> 1)компиляция geometry_cpp.obj и
  link t1_..cpp/main.exe .. main.obj geometry_cpp.obj 2)прогон тестовой цепочки для первой с++ задачи 3) компиляция geometry_cpp_dll.obj
  4)линк /common_cpp.dll. потому что только у t1_...cpp/main.exe прямая зависимость от geometry_cpp.obj, а у него - от geometry.hpp.
  и внутри main работа только с .x .y Point-a - при добавлении pad они просто сдвигаются в памяти, и это ничего не нарушает. 

  но хотелось бы ещё и пересборку для main t3-t4, т.к. там тоже есть include geometry.hpp. и тестовые цепочки к каждой задаче

  при запуске отдельно main.exe к t3 cpp на каких-нибудь тест. данных всё упало и asan вывел сообщение - что Point max_border = 24 байта,
  (хотя с double pad он уже успел стать 32 байта, это так в common_cpp.dll) а я пытаюсь обратиться к .z который теперь уже за границами 24 байтов.
  main.exe не обновляется и "внутри" остаётся старый .hpp, судя по которому struct Point = 24 байта, а для dll struct Point уже 32 байта

  сделала так, что Obj (в паттерн правиле) завият от всех заголовочников своей стороны. для t1-t2 там есть и лишние заголовочники. посоветовалась
  с дипсиком, он сказал что особенно на моём масштабе проекта это не критично

- возникла ситуация из-за локальных правок, которые забыла закоммитить (добавила комментарий в geometry.c только локально).
  после не делала make clean, make test ... 
  в итоге при make test пересобрался и geometry_c_dll.obj, и конечно он новее common_c.lib. запускается пересборка
  common_c.dll, а common_c.lib в этом случае остаётся по содержанию идентичным пердыдущему, и не перезаписывается т.к. линкер проверяет lib
  именно по содержанию (гипотеза deepseek судя по происходящему) - то есть логика пересборки make (timestamp) != логика link либы (по содержанию).
  
  ошибки нет, но это нечестно - из-за возраста lib-ы dll пересобирается даже когда это и не нужно. посоветовалась с дипсиком, стоит ли это исправлять. да, он советует это исправить т.к. это "дефект обвязки сборки". 
  + он говорит что это вызывает путаницу (что так и есть, т.к. я меняла только geometry.hpp а обновляется common_c.lib при корректном makefile, это
  выглядит очень не интуитивно) и на параллельной сборке это может вызвать конфликт - типа в одном потоке тест, использующий .dll, а во втором возникает пересборка .dll из-за старой .lib, и make всё равно что там у неё в содержании. и будет коллизия т.к. нельзя записать в файл, который
  открыт другим процессом

- дописала .DEFAULT_GOAL := all т.к. до этого make без цели делал создание первой папки: build/debug/t1_dist_matrix_cpp

- завела деп-лист заголовочников и условная копиляция в это моменте, т.к. он для виндоус, на линуксе он будет пустым (там через .d)

- по параграф 5 п.15 на линуксе другой инструмент для этого, поэтому добавила -MMD -MP в CXXFLAGS, CFLAGS. также, в часть линукса добавила
  -include $(wildcard $(BINDIR)/*/*.d) чтобы подтягивались .d; дипсик сказал, что без этого .d бесполезны т.к. они будут делаться, но включаться
  в состав makefile не будут

- сколонировала себе в чистую папку - проходит. то же самое под виртуалкой - проходит, и в каждой папке рядом с каждым .o есть свой .d

- сделала make -j8 на чистом клоне. параллельно пишется в vc140.pdb, который лежит в корне проекта; в нем где собрана информация о типах данных,
  которую надо было бы делать для каждого исходного файла, но она бы дублировалась, поэтому есть смысл её вынести в vc140.pdb
  при последовательном выполнении оно должно было перезаписываться каждый раз, но сейчас - конфликт т.к. несколько cl хотят записать одновременно.
  (26.08: прочитала что информация в vc140 не "перезаписывается", а именно дописывается)

  vc140 был там давно и я думала, что это старый артефакт от какой-то сборки, не проверяя, и забыла про него. хотя стоитло проверить дату

# 2026-08-26
*Что сделано*

- fatal error C1041 при make -j8. предалаемое компилятором решение - /Fs - принудительная синхронная запись. обход блокировки vc140 через то,
  что каждый cl отдаст инф. о типах в службу mspdbsrv.exe, та упорядочит записи и потом за один заход запишет в vc140; хорошо при записи в уже существующий файл. но есть несколько cl-ов пытаются создать vc140, то уже может быть ошибка; 
  
  make clean + make -j8 - худший случай, значит надо ориентироваться именно по нему. пока Fd плох
  только тем, что будет +расход места на мета-иформацию для каждого pdb-шника. deepseek говорит что в рамках моего проекта это копейки. 

  с /FS при первом прогоне: warning LNK4099 PDB... компоновка объекта выполняется как при отсутствии отладочных данных
    deepseek рассказал почему pdb может "теряться" - из-за окна между готовым объектником и готовым pdb - т.к. информация о типах
    временно находится в сервисе. либо пересоздание pdb новым cl - тогда инф. из старого pdb полностью теряется и объектники ссылаются
    на pdb который уже не хранит инф. об их типах. тогда нормальная отладка будет невозможна

  не вижу смысла использовать флаг, при котором конфликт прописан в документации и неизбежен при моём построени проекта:
  deepseek сказал что VC исп. FS при другой архитектуре, не как у меня, когда всё вливается в один vc pdb; буду использовать /Fd, мне кажется это архитектурно правильней, без конфликтов и информация о символах не потеряется

  по итогу размеры. при FS vc140.pdb = 0.99мб и при Fd все .obj.pdb ~= 4.72Мб

- убрала /Zi из служебных программ т.к. их не нужно отлаживать, они сами для тестирования

- под виндоус make -j8 выполняется, на виртуалке ubuntu - тоже

- убрала clang-format из PATH через where clang-format (C:\Program Files\LLVM\bin\clang-format.exe) и set PATH=%PATH:C:\Program Files\LLVM\bin=%
  при запуске make test как обычно выводится clang-format ... headers. потом 
  ```
  process_begin: CreateProcess(NULL, headers) failed.
  make (e=2): Не удается найти указанный файл.
  make: *** [Makefile:797: format-check] Error 2 
  ```
  с учётом, что это именно вопрос форматирования (не функциональность), то я не думаю что тест должен именно падать если у пользователя нет 
  clang-format. мне кажется, стоит выводить предупреждение, что файлы проекта не могут быть проверены на формат и стоит установить clang-format
  чтобы это происходило. и потом прогонять тесты t1-t4 после предупреждения

*Что заметила при работе с deepseek*
- когда решала, /FS или /Fd, попросила не давать готового решения. он хорошо давал наводящие вопрос, причём не так, что сразу очевидно, что Fd
  лучше. то есть он посоветовал подумать, "что более близко к тому, как работает VS?" "что даёт большую стабильность?" "Fs - одна правка makefile, Fd - больше".   

- посоветовал убрать Zi из run_case и check - служебные программы. спросила нет ли противоречию политике курса?
  сказал нет, т.к. это мои инструменты для тестирования, но не то, что подвергается тестироваю и отладке

# 2026-08-27
*Что сделано*
- написала format-check-wrapper который будет выводить предупреждение если у пользователя нет clang-format-a или делать проверку, если есть.
  тесты самих задач будут происходить в любом случае
  
  не буду плодить сущности, пусть всё живёт в format-check, а format-check-wrapper не нужен, он дублировал поведение

  сообщения из format-check будут на английском, т.к. в виндоус всё решалось просто через chcp 1251 в начале. но т.к. хочется поддерживать
  кросплатф. с линукс, а там консоль в UTF-8, дипсик рассказал. значит надо менять кодировку makefile, чего делать не хочется. или настраивать терминал, или хранить отдельный файл с предупреждением в нужной кодировке (это лишний файл, и под винду нужен будет либо второй, либо также 
  оставить вывод через echo). 
  эти варианты мне не нравятся, так что лучше предупреждения на английском

- протестировала на линукс. убирала clang не через PATH, а
  ```
  sudo  mv /usr/bin/clang-format /usr/bin/clang-format.disabled
  hash -r
  make test -> clang-format wasnt found... ALL TESTS PASSED
  sudo mv /usr/bin/clang-format.disabled /usr/bin/clang-format
  hash -r
  ```
  и ситуацию, когда формат испорчен и clang-format имеется - чтобы make именно падал с ошибкой. так и происходит. то же самое на виндоус

- ввод лишних аргументов в задаче 3
  - t3_cpp + 1 2 3 4 -> печать стандартная(кол-во точек: 1 ...), код выхода = 0;
  - t3_cpp + 1 2 3 x -> печать стандартная(кол-во точек: 1 ...), код выхода = 0;
  - t3_c + 1 2 3 4 -> печать ошибки(Строка 1. Ожидались координаты X Y Z. Получено: 1 2 3 4), код выхода = 65;
  - t3_c + 1 2 3 x -> печать ошибки(Строка 1. Ожидались координаты X Y Z. Получено: 1 2 3 x), код выхода = 65;

  права задача на Си по пункту по ex-коды из постаноки задачи - она выдаёт exit-код 65 при отхождении от ожидаемого ввода,
  то есть у неё верно с машинным каналом. но сейчас мне кажется, у неё есть и проблема - с stderr. потому что она не детализирует проблему.
  а канал для человека должен детализировать, что именно не так. у меня сейчас одно сообщение на всё. и с++ версия игнорирует
  всё после X Y Z. я замечала несостыковку в моменте, что при бОльшем к-ве координат несостыковка и даже записывала её 2026-08-21. 
  потом продолжила делать Makefile. надо было отметить как TODO или типа того, потому что по итогу я просто забыла про это

  подумала над исправлением и что будет возникать проблема, например если "1 2 3 4 x" - это проблема в символе или лишней координате?
  мне показалось логичным кидать ошибкой то, что первым встретилось при парсе, в случае "1 2 3 4 x" - лишний аргум., "1 2 3 x 4" - символ.
  спросила у deepseek, он сказал это ок и такой принцип есть у парсеров компиляторов, наверное он мне оттуда и вспомнился.
  и "1 2 3 4r" и 1 2 3 r4" оба будут плохи именно из-за символа, то есть буду смотреть на целый токен после XYZ

- parse_point geometry.cpp / geometry.c теперь возвращает 1 для "недостаточно" и 3 для "слишком много". код ошибки так же 65.
  поменяла под это 3 задачу и 4 тоже. изменила под это expected в тестах.
  parse_point geometry.cpp поменяла условие *ptr != '\0' на ptr != ptr_end, т.к. deepseek рассказал, что string_view не обязан
  заканчиваться на \0

  в 4 задаче изменила сообщение при нечисловом вводе, чтобы было единообразно, как и в 3 задаче. поменяла и в тексте expected у тестов

  в тестовых данных для t3-4 расставила разные пробелы после входных данных. т.к. падать должно именно от данных после X Y Z, а не от пробелов
  
  вынесла "магические числа" в parse_point и коды возврата при распознавании радиуса в t4 cpp

  сделаю случай для inf/nan в точках. причём если inf/nan среди X Y Z, то это будет отдельный вывод stderr "не-конечное число".
  а если inf/nan после X Y Z, то это будет восприниматься как лишнее число 

*Что не получилось*
- нужно дописать тесты на все эти случаи - лишие числа и символы во всех вариациях и nan / inf, + тест клона и на линуксе

- t4 си печатает проходящу точку хотя возможно она ничего не должна печатать
  ```
  c:\Users\User\Desktop\inobitec_stud>set "PATH=build\debug\common;%PATH%" & build\debug\t4_filter_c\main.exe 5
  1 2 3
  1 2 3 4
  Строка 2 - слишком много координат. Ожидалось X Y Z, получено: 1 2 3 4
  1.000 2.000 3.000
  ```
  так же и при нечисловых данных

*Что заметила при работе с deepseek*
- в format-check-wrapper посоветовал в удачной ветке заменить make format-check на $(MAKE) format-check. поговорили при рекурсивный make

  тут у deepseek-а было много ошибок: например он сначала сказал, что make в отличие от $(MAKE) не наследует MAKEFLAGS, потом пришёл к тому,
  что всё-таки наследует. но это разные экземпляры make (из PATH / текущий). потом сказалч, что при -n у родителя даже с $(MAKE) 
  не будет выведена вложенная работа. попробовала так сделать - выводится. не буду развивать с ним эту тему дальше, лучше gnu manual

  там написано что recursive make - когда есть вложенные проекты со своим makefile. то есть цель рекурсии сразу вводится вот так, а это не мой
  случай. также, дипсик высказал тезис "своя же цель должна жить в пререквизитах" - вот этому я верю, т.к. это повторение всей философии make.
  тажке, если вот так вызывать make из рецептов - это выход из графа (из-за этого, например, make не будет следить за тем, что внутри $(MAKE) 
  и даже если там одно и то же действие - оно выполнится несколько раз. потому что что, что в $(MAKE) - не в дереве).
  тут граф один, нет подпроектов - тоже верю, это мысть из gnu manual выше.
  потом переспросила оба этих момента, он тоже в них уверен.

- когда попросила перепроверить за мной при исправлении сиутации в t3-t4 с обработкой "x y z что-то ещё", он посоветовал разобраться и со
  случаем inf/nan в точках, и нашёл много маленьких моментов для исправления - сделать проверку argc в t4 Си единообразно с t4 c++;
  ввести константы для магических чисел в parse_point и t4 c++ обработка радиуса; использовать в t4 Си hypot а не считать рукми (тут он
  наврал, в Си hypot только для 2мерного пространства); 

## 8. Диалоги с DeepSeek

**Что:** полный машинный экспорт переписки с DeepSeek через OpenCode — JSON со всем содержимым сессии: реплики, рассуждения, **полный вызов каждого инструмента**, временные метки. **Без обработки, без выжимок, без редактуры, без ручной транскрипции** — обработка разрушает сигнал, который наставник в логе ищет.

**Зачем:** наставник смотрит манеру общения с ИИ — какие вопросы задаёшь, замечаешь ли галлюцинации, применяешь ли анти-подхалимажные приёмы. По манере видно: ты извлекаешь навык, или ИИ делает работу за тебя. Это основной канал, по которому наставник даёт рекомендации.

**Как выгружать — штатная команда OpenCode, руками (не просить DeepSeek сделать за тебя):**

```powershell
opencode session list                                  # найти ID сессии
opencode export <session-id> > dialog_logs/2026-06-15_task1.json
```

Каталог `dialog_logs/` — в корне репо, коммитится вместе с кодом. **В именах файлов не использовать `:`** (NTFS не примет у получателя) — дата и тема через `_` и `-`.

**Когда выгружать — СРАЗУ после сессии**, не в конце недели: OpenCode автоматически чистит старые тела сообщений; не выгрузил сразу — экспорт может вернуть пусто. **Дисциплина сессий: новая задача/тема = новая сессия**, не одна накопительная на месяц — каждый лог про одну тему.

**Что НЕ делать:** пересказы, конспекты, рефлексию пост-фактум, редактуру JSON. Опечатки и неудачные попытки — то самое сырьё. Анализ — работа наставника; твоя рефлексия встроена в сам диалог (приёмы § 4.2) в момент его ведения.
